# TASKS.md — 티켓 목록

각 티켓은 30분~1시간 분량. **티켓 하나를 끝내면 멈추고 확인을 요청**한다.
완료 조건은 사용자가 `npm run dev` 서버 URL을 열어 **직접 플레이하며 확인**할 수 있는 문장으로 적었다.

## 새 세션 시작 절차 (Claude용)

1. `CLAUDE.md` → `SPEC.md` → 이 파일 순서로 읽는다. `git log --oneline -5`로 마지막 커밋 티켓을 확인한다 (맨 아래 "진행 현황" 표와 대조)
2. 개발 서버: `preview_start {name:'dev'}`(`.claude/launch.json`). 5173을 다른 세션 서버가 쥐고 있으면 같은 폴더를 서빙하므로 `navigate http://localhost:5173/`로 그냥 연다. Node는 Git Bash PATH에 없으므로 매 Bash 호출에 `export PATH="/c/Program Files/nodejs:$PATH"`를 붙인다
3. 다음 티켓은 플랜 모드로 계획 → 승인 → 구현. 단계 첫 티켓이면 SPEC의 TBD 인터뷰(기획 질문만)부터
4. 검증은 Node 로직 테스트 + 헤드리스 Chrome 스크린샷(`chrome.exe --headless=new --use-angle=swiftshader --screenshot`). Claude Code 브라우저 창은 포인터 락·프레임 제한으로 실플레이 검증이 안 된다 — 감각 확인은 사용자
5. 사용자 확인 후 `T00 제목`으로 커밋. 단계가 끝나면 SPEC/TASKS 갱신 커밋

**현재 위치 (2026-09-07)**: **1~5단계 완료** (T28 결과 화면 `7d809f4`, T40 제외). 2026-09-07 사용자 에픽 4개로 **6단계 T41~T49 추가** (아래 "6단계" 절). 순서 **T41 → T42 → T44 → T43 → T45 → T46 → T47 → T48 → T49**. **다음은 T41** (플랜 모드로 시작). 5단계 종료 갱신(SPEC 5·7·8장, T26.4·T32·T38 완료 조건 문구)은 아직 안 했다 — T41 전에 한다.

대기 중인 인터뷰 — 5단계 인터뷰는 전부 확정됐다 (SPEC 7-1·8장). **6단계에서 시작 시 물어야 하는 티켓**: T45(CS 카드 이름·환급 레벨·멀티킬 유지), T47(웨이브 표 확정), T48(음원 파일·라이선스), T49(점검 목록 승인). 각 티켓 본문에 질문 목록이 있다.

정조준 FOV(TBD #2)는 59 유지, 사용자 통보 대기 중.

배경이 필요하면 [docs/킥오프 원문.md](docs/킥오프%20원문.md)와 [docs/결정 로그.md](docs/결정%20로그.md)를 본다.

---

# 1단계 — 1인칭 이동

## T01. 프로젝트 세팅과 빈 3D 화면
**단계** 1 · **의존** 없음

**할 일**
- 폴더 구조안을 제안하고 승인받는다 (SPEC TBD #1)
- Vite + Three.js(버전 고정) 설치, `npm run dev` / `npm run build` 스크립트 구성
- `npm run build` 결과가 `dist/index.html` 한 개가 되도록 설정
- `git init`, `.gitignore` 작성
- 회색 바닥과 하늘색 배경만 있는 빈 3D 화면을 띄운다

**완료 조건**
- `npm run dev`로 뜬 주소를 열면 회색 바닥이 보이는 3D 화면이 나온다
- `npm run build` 후 `dist/index.html`을 **더블클릭하면 같은 화면이 뜬다**

---

## T02. 그레이박스 테스트 공간과 카메라 눈높이
**단계** 1 · **의존** T01

**할 일**
- 벽으로 둘러싼 사각 공간, 안에 상자·기둥 몇 개를 배치한다 (거리감을 볼 수 있는 정도)
- 1인칭 카메라를 눈높이 1.7m에 둔다
- 지향 FOV 90°를 적용한다

**완료 조건**
- 화면이 사람 눈높이에서 방 안을 보는 시점이다
- 방 안에 상자와 기둥이 보이고, 벽 끝까지 시야에 들어온다

---

## T03. 마우스 룩과 감도
**단계** 1 · **의존** T02

**할 일**
- 화면 클릭 시 마우스 잠금, ESC로 해제
- 마우스 이동으로 시점 회전. 단위는 **°/100px**, 기본 8.0
- 위아래 회전은 천장/바닥을 넘어가지 않도록 제한
- 감도 조절 슬라이더(범위 1~30)를 화면에 둔다

**완료 조건**
- 화면을 클릭하면 마우스 커서가 사라지고 마우스를 움직이는 대로 시점이 돈다
- 위아래로 끝까지 돌려도 화면이 뒤집히지 않는다
- 슬라이더를 오른쪽으로 밀면 같은 마우스 움직임에 시점이 더 많이 돈다
- ESC를 누르면 커서가 돌아온다

---

## T04. WASD 이동, 질주, 웅크리기
**단계** 1 · **의존** T03

**할 일**
- WASD 이동, 걷기 5.5 m/s
- Shift 질주 ×1.5 (누르는 동안)
- C 웅크리기 ×0.5 (토글), 웅크리면 눈높이 1.0m
- 시작·정지에 짧은 가속·감속을 넣는다. 점프는 넣지 않는다

**완료 조건**
- WASD로 바라보는 방향 기준으로 움직인다
- Shift를 누르면 눈에 띄게 빨라지고, 떼면 원래 속도로 돌아온다
- C를 한 번 누르면 시점이 낮아진 채 유지되고, 다시 누르면 일어선다. 웅크린 상태에서 이동이 느리다
- 키를 떼도 즉시 멈추지 않고 아주 짧게 미끄러진 뒤 선다

---

## T05. 벽 충돌과 미끄러짐
**단계** 1 · **의존** T04

**할 일**
- 플레이어와 벽·상자·기둥의 충돌 처리
- 벽에 비스듬히 부딪히면 멈추지 않고 벽을 따라 미끄러지게 한다

**완료 조건**
- 벽을 향해 걸어가면 통과하지 못하고 막힌다
- 벽에 비스듬히 밀어붙이면 멈추지 않고 벽을 따라 계속 미끄러져 이동한다
- 상자와 기둥에도 똑같이 막힌다

---

## T06. 이동 수치 조절 패널
**단계** 1 · **의존** T05

**할 일**
- 걷기 속도 / 질주 배율 / 웅크리기 배율 / FOV / 감도를 화면에서 조절할 수 있게 한다
- 정조준 FOV 실수치를 확정한다 (SPEC TBD #2)
- 수치들을 한 곳(설정 값 모음)으로 모은다

**완료 조건**
- 화면의 패널에서 FOV를 바꾸면 시야가 즉시 넓어지거나 좁아진다
- 이동 속도를 바꾸면 다음 이동부터 바로 반영된다
- 값을 여러 개 바꿔가며 "이 정도가 맞다" 싶은 조합을 찾아 확정할 수 있다

---

# 2단계 — 사격 시스템 (기준 AR 하나만)

## T07. 무기 데이터 파일과 로더
**단계** 2 · **의존** T06

**할 일**
- `docs/총기 시뮬레이터.html`의 `WEAPONS`에서 **기준 AR** 값을 꺼내 무기 데이터 파일로 만든다
- 3D용 신규 필드 자리를 만든다: 피해량, 헤드샷 배율, 사거리 감쇠, 재장전 시간, 이동 퍼짐 배율
  → 실수치는 이 티켓 시작 시 인터뷰로 확정 (SPEC TBD #3~7)
- 로더가 값을 검증한다. 빠지거나 잘못된 값은 **화면에 경고를 띄우고 기본값으로 진행**

**완료 조건**
- 화면 어딘가에 현재 불러온 무기 이름과 주요 수치가 표시된다
- 데이터 파일에서 값 하나를 일부러 지우면, 게임이 멈추지 않고 화면에 어떤 값이 잘못됐는지 경고가 뜬다

---

## T08. 반동 엔진 — 패턴 누적
**단계** 2 · **의존** T07

**할 일**
- 패턴 컴파일: `curve` 모드(v0/vG/vMax/h0/hMode/hMax)와 `array` 모드 둘 다 지원
- 발사할 때마다 수직·수평 반동을 카메라 회전에 누적
- `randV` / `randH` 무작위 적용. 수직은 아래로 내려가지 않게
- `cap` 상한 클램프
- 패턴 인덱스 순환 (초과 시 뒤 1/3 반복, 사격 정지 500ms 후 리셋)

**완료 조건**
- 마우스 왼쪽 버튼을 누르고 있으면 화면이 위로 밀려 올라간다
- 계속 쏘면 좌우로도 흔들리고, 30발을 넘겨도 패턴이 끊기지 않고 이어진다
- 손을 떼고 잠시 뒤 다시 쏘면 첫 발부터 다시 시작하는 느낌이 난다

---

## T09. 반동 복귀 (recovery)
**단계** 2 · **의존** T08

**할 일**
- 사격을 멈추고 `delay`(ms)가 지나면 누적 반동을 `speed`(°/s)로 되돌린다
- **보정분 제외** — 총이 밀어올린 반동만 되돌리고, 플레이어가 마우스로 내린 각도는 되돌리지 않는다

**완료 조건**
- 한 탄창을 쏘고 손을 떼면 화면이 스스로 원래 높이로 서서히 내려온다
- 쏘는 동안 마우스로 화면을 끌어내린 뒤 손을 떼면, 끌어내린 만큼 다시 올라가지 않는다

---

## T10. 탄퍼짐 (spread)
**단계** 2 · **의존** T09

**할 일**
- `base` + `bloom` 누적, `max` 상한, `decay` 회복. **사격 중에는 ×0.25로 느리게 회복**
- 탄착점을 **원형 균등 분포**로 흩는다 (`sqrt(random)` 반지름)
- 이동 속도에 비례해 퍼짐이 커지게 한다 (무기별 배율)
- 조준선이 현재 퍼짐에 따라 벌어지고 좁아지게 한다

**완료 조건**
- 오래 연사할수록 조준선이 점점 벌어지고, 손을 떼면 다시 좁아진다
- 가만히 서서 쏠 때보다 걸으면서 쏠 때 조준선이 더 벌어진다
- 조준선이 벌어진 상태에서 벽에 쏘면 탄자국이 넓게 흩어진다

---

## T11. 정조준 (ADS)
**단계** 2 · **의존** T10

**할 일**
- 우클릭을 **누르는 동안** 정조준. `ads.time`에 맞춰 FOV를 보간(해제는 1.4배 빠르게) — **사용자 메모 1: 즉시 줌이 아니라 부드럽게.** T06의 즉시 전환 미리보기를 이것으로 대체
- 정조준 진행도에 따라 반동 배율(`ads.recoil`), 퍼짐 배율(`ads.spread`)을 보간 적용
- `ads.allowed`가 꺼진 무기는 정조준이 아예 동작하지 않게 한다

**완료 조건**
- 우클릭을 누르면 화면이 부드럽게 확대되고, 떼면 조금 더 빠르게 원래대로 돌아온다
- 정조준한 채로 쏘면 지향사격보다 화면이 덜 튀고 조준선이 덜 벌어진다
- 확대되는 도중에 쏴도 어색하지 않게 중간 값으로 동작한다

---

## T12. 발사·탄약·재장전
**단계** 2 · **의존** T11

**할 일**
- `rpm` 간격 풀오토 발사, 탄창 소모, 예비탄은 무한
- R키 재장전, 재장전 시간 적용
- **빈 탄창에서 사격 입력 시 자동으로 재장전** 시작
- 재장전 취소: 무기 교체 / 질주 / 사격 입력 / 정조준 — 4가지 모두
- **질주 중에는 사격 불가**. 사격 버튼을 누르면 질주가 풀리고 바로 발사된다
- 탄약 UI(현재/최대)와 재장전 진행 표시

**완료 조건**
- 탄창을 다 비우면 총이 멈추고, 그 상태에서 다시 쏘면 알아서 재장전이 시작된다
- 재장전 중에 Shift로 달리거나 우클릭하거나 사격 버튼을 누르면 재장전이 끊긴다
- Shift로 달리는 중에 사격 버튼을 누르면 달리기가 풀리면서 곧바로 총이 나간다
- 화면에 남은 탄약 수가 보이고, 재장전 중에는 진행 상태가 보인다

---

## T13. 레이캐스트 판정과 표적
**단계** 2 · **의존** T12

**할 일**
- 표적을 만든다 — HP + 몸통 콜라이더 + **머리 콜라이더**. 구성은 이 티켓 시작 시 확정 (SPEC TBD #8)
- 레이캐스트로 명중 판정, 피해량 적용, HP 0이면 표적 반응
- 거리에 따른 피해 감쇠, 머리 명중 시 배율 적용

**완료 조건**
- 표적을 쏘면 맞은 자리에 반응이 나고 HP가 줄어든다
- 머리를 맞히면 몸통을 맞혔을 때보다 HP가 더 많이 줄어든다
- 멀리 있는 표적을 맞히면 가까운 표적보다 HP가 덜 줄어든다
- HP가 0이 되면 표적이 쓰러지거나 사라진다

---

## T14. 명중 피드백
**단계** 2 · **의존** T13

**할 일**
- 트레이서 — 총구에서 탄착점까지의 짧은 선
- 히트마커 — 명중 순간 조준선 표시, 머리 명중은 구분되게 — **사용자 메모 2: 히트했을 때 히트 마커가 뜰 것**
- 데미지 숫자 — 맞은 위치에 떠올랐다 사라지게
- 벽 탄착 자국

**완료 조건**
- 쏠 때마다 총구에서 목표까지 선이 스쳐 지나간다
- 표적을 맞히면 조준선에 표시가 뜨고, 머리를 맞히면 다른 모양/색으로 뜬다
- 맞은 자리에 피해량 숫자가 떠올랐다가 사라진다
- 벽에 쏘면 탄자국이 남아 탄착군을 눈으로 볼 수 있다

---

# 3단계 — 총기별 반동 (핵심)

## T15. 4정 데이터 이식
**단계** 3 · **의존** T14

**할 일**
- 시뮬레이터에서 **CS형 / PUBG형 / COD형 / 데스티니형** 값을 꺼내 무기 데이터에 추가한다
- CS형의 30발 고정 배열(`array` 모드)이 제대로 동작하는지 확인한다
- CS형의 `ads.allowed = false`가 실제로 정조준을 막는지 확인한다
- 기준 AR은 사격장에 올리지 않는다

**완료 조건**
- 무기 데이터에 4정이 들어 있고, 각각의 이름과 수치가 화면에 표시된다
- CS형을 들고 우클릭해도 화면이 확대되지 않는다

---

## T16. 무기 전환
**단계** 3 · **의존** T15

**할 일**
- 숫자키 1/2/3/4로 직접 전환, 마우스 휠로 순환 전환
- 전환 시 반동 누적·bloom·패턴 인덱스를 초기화하고 재장전을 취소한다
- 현재 무기 이름과 수치를 화면에 표시

**완료 조건**
- 1/2/3/4를 누르면 그 총으로 바뀌고, 휠을 굴려도 순서대로 바뀐다
- 한 총으로 연사해 화면이 밀려 올라간 상태에서 다른 총으로 바꾸면 화면이 원래대로 돌아온다
- 재장전 중에 총을 바꾸면 재장전이 끊긴다

---

## T17. 3D 사격장
**단계** 3 · **의존** T16

**할 일**
- 사격장 공간을 만든다 — 사격 라인, 거리별 표적, 탄착을 볼 벽
- 표적 거리와 배치를 이 티켓 시작 시 확정 (SPEC TBD #9)
- 거리 표시(예: 10m / 25m / 50m)를 바닥이나 표적에 붙인다

**완료 조건**
- 사격 라인에 서면 가까운 표적과 먼 표적이 한눈에 보인다
- 각 표적까지의 거리가 화면 또는 바닥에 적혀 있다
- 정면 벽에 대고 쏘면 탄착군이 그대로 남는다

---

## T17.5. 무기 튜닝 패널 (3단계 중 추가)
**단계** 3 · **의존** T17

**할 일**
- ESC로 잠금을 푼 상태에서 현재 무기의 값을 슬라이더로 바꿔 즉시 느껴본다
- 바꾼 값은 새로고침해도 유지, "파일 값으로 초기화", "JSON 복사"(weapons.json 항목 형식)

**완료 조건**
- 슬라이더를 움직이면 그 자리에서 총의 반동·퍼짐·복귀·정조준이 바뀐다
- 새로고침해도 바꾼 값이 남아 있고, 초기화를 누르면 파일 값으로 돌아온다

---

## T18. 검증 UI ① 탄착군 시각화
**단계** 3 · **의존** T17

**할 일**
- 탄착 자국을 누적해 보여준다 (일정 개수 유지)
- 탄착군을 지우는 버튼/키를 둔다
- 사격장에서는 항상 켜져 있다

**완료 조건**
- 한 탄창을 쏘면 벽에 탄착군 모양이 그대로 남는다
- 총을 바꿔 다시 한 탄창을 쏘면 앞 총과 모양이 다르다는 것이 눈으로 보인다
- 지우기를 누르면 탄자국이 사라져 다음 비교를 깨끗하게 시작할 수 있다

---

## T19. 검증 UI ② 이론 패턴 오버레이
**단계** 3 · **의존** T18

**할 일**
- 현재 무기의 반동 궤적(무작위 없는 이론값)을 화면에 겹쳐 그린다
- 지금 몇 번째 탄인지 궤적 위에 표시한다

**완료 조건**
- 화면에 그 총이 앞으로 어디로 밀릴지 궤적이 미리 그려져 있다
- CS형을 들면 30발 궤적이 정확히 고정된 모양으로 보이고, PUBG형을 들면 좌우가 무작위라 궤적과 실제 탄착이 어긋나는 것이 보인다
- 쏘는 동안 현재 탄 번호가 궤적을 따라 이동한다

---

## T20. 검증 UI ③ 수치 패널 — **생략** (사용자 결정 2026-09-04)
**단계** 3 · **의존** T19
탄착군·이론 궤적·무기 정보 패널로 충분하다고 판단해 만들지 않았다. 아래는 원래 계획.

**할 일**
- 실시간으로 표시: 현재 누적 반동(°), 현재 퍼짐(°), 정조준 진행도, 남은 탄, 발사 번호
- 무기의 주요 세팅값(rpm, 복귀 delay/speed, spread base/bloom 등)도 함께 표시
- 사격장에서는 항상 켜져 있다

**완료 조건**
- 쏘는 동안 누적 반동 숫자가 올라가고, 손을 떼면 다시 0으로 내려간다
- 우클릭을 누르면 정조준 진행도가 0에서 1로 차오르는 것이 숫자로 보인다
- 총을 바꾸면 패널의 세팅값이 그 총의 값으로 바뀐다

---

## T21. 산출물 문서 3종
**단계** 3 · **의존** T20

**할 일**
- `docs/`에 마크다운 3개를 쓴다
  1. **총기 감각 구성요소 분해** — 반동/퍼짐/복귀/ADS가 각각 감각의 어떤 축을 담당하는가
  2. **총기별 세팅 비교표** — 3D에 올린 4정만. 값 + 그 값이 만든 감각/행동 변화
  3. **신규 총기 기획 절차** — 새 총 한 정을 추가할 때 무엇을 어떤 순서로 정하는가

**완료 조건**
- `docs` 폴더에 마크다운 3개가 있고, 각각을 열면 표와 설명이 읽힌다
- 비교표의 수치가 게임에서 실제로 쓰는 값과 일치한다
- 기획 절차 문서를 따라가면 새 총 한 정의 값을 빈칸 없이 채울 수 있다

---

# 4단계 — 스테이지 + 몬스터 (굵은 단위)

## T22. 몬스터 2종
**단계** 4 · **의존** T21
스탯, 피격 반응, 사망 처리. 근접형과 원거리형. 상세 수치는 시작 시 인터뷰 (SPEC TBD #10 → 6-1 표로 확정)
**완료 조건** — 구역에 서 있는 몬스터를 쏘면 피격 반응이 나고, HP가 다하면 죽는다. 근접형은 다가와서 때리고 원거리형은 떨어져서 공격한다

## T23. 트리거 존 스폰과 웨이브
**단계** 4 · **의존** T22
특정 위치에 들어서면 몬스터가 웨이브로 등장. 웨이브 구성은 코드로 관리 (확정: 3웨이브, 5초 카운트다운. T25에서 트리거를 구역 입구로 이동)
**완료 조건** — 구역 안쪽으로 걸어 들어가면 몬스터가 등장하고, 다 처치하면 다음 웨이브가 나온다. 마지막 웨이브를 처치하면 더 나오지 않는다

## T24. 플레이어 HP와 사망
**단계** 4 · **의존** T23
HP, 피격 표시, 사망 시 **그 자리에서 즉시 부활** (확정: HP 1000 무회복, 비네트+방향 호, 2초 뒤 부활 + 현재 웨이브 재스폰)
**완료 조건** — 몬스터에게 맞으면 화면에 피격 표시가 나고 HP가 줄어든다. HP가 0이 되면 잠깐 뒤 같은 자리에서 다시 시작한다

## T25. 그레이박스 구역 1개
**단계** 4 · **의존** T24
엄폐물·높이차가 있는 전투 구역 하나를 만든다 (확정: 백스톱 문 뒤 30×30 방, 낮은 상자 + 높은 벽, 높이차는 **몬스터 전용 단**만)
**완료 조건** — 엄폐물 뒤에 숨어 원거리형의 공격을 피할 수 있고, 나와서 반격할 수 있다

---

# 5단계 — 스테이지 진행 (굵은 단위)

## T26. 스테이지 흐름
**단계** 5 · **의존** T25
사격장 → 구역 3개 → 보스. 구역 클리어 조건과 다음 구역 이동
**완료 조건** — 사격장에서 시작해 구역을 하나씩 클리어하며 앞으로 나아가고, 마지막에 보스를 만난다

---

# UI 정리 (2026-09-05 사용자 지시 — T27·T28 앞에 먼저 진행)

사용자 지시로 T26.1~T26.4를 추가한다. 5단계 순서(T27 → T28)는 그대로이고, 이 네 개를 먼저 한다.
공통 원칙: **빼낸 요소는 지우지 않고 숨긴다** (다음 티켓들이 옮겨 쓴다). 사격 룰·수치는 건드리지 않는다.

## T26.1. 상시 HUD 비우기
**단계** 5(중간) · **의존** T26
시작 직후 화면에 텍스트 블록이 없게 한다. 지금 화면에 상시로 떠 있는 것들을 숨기고, 플레이에 꼭 필요한 요소만 남긴다.

**숨기는 것** (DOM은 유지, `hidden` 처리 — T26.2·T26.4가 다시 쓴다)
- 설정 패널(`settingsPanel`) 전체: 긴 안내문, 감도·이동·FOV 슬라이더, 탄착군 지우기·이론 궤적·리셋 버튼, 몬스터 접이식 그룹, **받은 공격 카운터**
- 무기 튜닝 패널(`tuningPanel`) 전체
- 무기 정보(`weaponInfo`)의 설명문(수치 7줄)과 라인업 4줄

**남기는 것**
- 조준점(원), 히트마커, 피해 숫자(우상단 스택), 플레이어 HP 바(중앙 아래)
- 상단 한 줄: 구역·웨이브·남은 몬스터 (지금 `waveHud`)
- **보스 HP 바** — 보스 구역에서만 뜨는 가로 바 (상단 한 줄 아래). 새로 만든다
- **무기 슬롯**(우하단): 아이콘(무기 색 네모) + 이름 + 탄창/예비(예비는 무한이면 `∞`) + 재장전 진행바. 그 외 글자 없음
- **중앙 상황 프롬프트**: "앞으로 가세요"(구역 클리어), "N초 후 부활"(사망), "보스 처치 — 데모 완료"만. 그 외 상시 문구 없음
- **데이터 경고**: 경고가 있을 때만 우상단에 작은 배지(예: `⚠ 2`). 눌러야 내용이 펼쳐진다. 없으면 아무것도 없음

**완료 조건**
- 게임을 열자마자 화면에 텍스트 블록(안내문·설명문·슬라이더·패널)이 하나도 없다. 보이는 글자는 무기 이름·탄약 숫자·HP 숫자뿐
- 구역에 들어가면 상단 한 줄이 뜨고, 보스 구역에서만 보스 HP 바가 뜬다
- 무기를 바꾸면 슬롯의 아이콘·이름·탄약이 바뀌고, R을 누르면 진행바가 찬다
- 클리어·사망 때만 중앙에 프롬프트가 잠깐 뜬다
- `data/weapons.json`을 일부러 망가뜨리면 우상단 배지가 뜨고, 정상이면 안 뜬다

## T26.2. 일시정지 메뉴 (ESC / Tab)
**단계** 5(중간) · **의존** T26.1
ESC 또는 Tab으로 열리는 전체 화면 오버레이. 열리면 포인터 잠금이 풀리고, 게임 루프는 계속 돌되 입력은 막힌다.

**내용**
- **조작키 표**: WASD 이동 · Shift 질주 · C 웅크리기 · 좌클릭 사격 · 우클릭 정조준 · R 재장전 · 휠 / 1~3 무기 전환(라인업 3정 — COD형 제외, 2026-09-05) · P 이론 궤적 · X 탄착군 지우기 · M 스테이지 리셋 · ESC/Tab 이 메뉴
- **데모 흐름** 서너 줄: 사격장에서 시작 → 백스톱 문 → 구역 1~3 → 보스. 구역의 웨이브를 모두 처치하면 다음 문이 열린다. HP 0이면 그 자리에서 부활하고 현재 웨이브가 다시 시작된다
- **버튼 셋**: `무기 세팅`(T26.4 패널 열기, 그 전까지는 기존 튜닝 패널을 연다) · `디버그`(T26.1에서 숨긴 설정 패널 — 감도·이동·FOV 슬라이더, 몬스터 레버, 받은 공격 카운터, 탄착군·궤적 버튼 — 을 다시 보인다) · `스테이지 리셋`(M과 같음)
- T26.1에서 뺀 안내문 내용은 여기 조작키 표·데모 흐름으로 흡수한다 (원문 그대로 붙이지 않는다)

**닫기**: ESC/Tab을 다시 누르거나 오버레이 바깥(게임 화면)을 클릭하면 닫히고 **포인터가 다시 잠긴다**. 디버그·무기 세팅 패널이 열려 있을 때는 그 패널만 닫고 메뉴로 돌아온다.

**완료 조건**
- 플레이 중 ESC 또는 Tab을 누르면 마우스가 풀리고 메뉴가 보인다. 조작키 표·데모 흐름·버튼 셋이 있다
- 다시 ESC를 누르거나 화면을 클릭하면 메뉴가 사라지고 바로 시점 조작이 된다 (추가 클릭 없이)
- `디버그`를 누르면 옛 설정 패널이 나타나고 슬라이더가 동작한다. `스테이지 리셋`은 M과 같다
- 메뉴가 열린 동안 사격·이동이 되지 않고 몬스터는 계속 움직인다 (일시정지가 아니라 메뉴 — 위험은 감수)

## T26.3. 무기 전환 카드
**단계** 5(중간) · **의존** T26.1
휠이나 숫자키로 무기를 바꾸면 **화면 하단 중앙에 1.5초** 동안 카드가 뜬다.

**내용**: 아이콘(무기 색) · 이름 · `탄창 30 · 발당 200 · 600 RPM` 한 줄 · **이 총이 뭐 하는 총인지 한 문장**
- 한 문장은 기존 `weaponInfo` 설명문을 압축한 것. 무기별 문장은 `data/weapons.json`에 `desc` 필드로 넣는다 (없으면 태그로 대체). 초안(Claude, 사용자 수정 가능). 라인업은 3정 (COD형 제외 — 사용자 결정 2026-09-05, SPEC 5장 '4정 확정'은 단계 종료 시 갱신):
  - CS형 — 무작위 없는 30발 고정 궤적. 외우면 완전히 제어된다
  - PUBG형 — 위로는 항상, 옆으로는 무작위. 복귀가 느려 끊어 쏘는 총
  - 데스티니형 — 순한 반동에 즉시 복귀. 사실상 자동 리센터
- 같은 무기를 다시 고르면 안 뜬다
- 빠르게 여러 번 전환하면 카드가 겹치지 않고 **마지막 무기 것만** 남고 1.5초를 다시 센다

**완료 조건**
- 휠을 한 칸 굴리면 하단 중앙에 카드가 떴다가 1.5초 뒤 사라진다. 숫자키도 같다
- 지금 든 무기의 숫자키를 누르면 아무것도 안 뜬다
- 휠을 세 칸 빠르게 굴리면 카드 하나만 보이고 마지막 무기 것이다

## T26.4. 무기 세팅 패널
**단계** 5(중간) · **의존** T26.2, T26.3 · **가장 큰 티켓**
기존 튜닝 패널(T17.5)을 대체하는 **우측 고정 폭 패널**. 일시정지 메뉴의 `무기 세팅`으로 연다. 상단에 무기 선택 드롭다운, 그 아래 탭 세 개.

**탭 구성과 JSON 키 대응** (이 탭 이름이 나중에 데이터 구조 문서의 층 이름이 된다)

| 탭 | 묶음 | 슬라이더 | `weapons.json` 키 | 비고 |
|---|---|---|---|---|
| 스펙 | | 발당 피해 | `damage` | |
| 스펙 | | 헤드샷 배율 | `headshotMul` | 기존 튜닝에 없던 항목 (신규) |
| 스펙 | | RPM | `rpm` | |
| 스펙 | | 탄창 | `mag` | |
| 스펙 | | 재장전 시간 | `reloadTime` | |
| 스펙 | | 정조준 가능 | `ads.allowed` | 체크박스 |
| 핸들링 | 반동 | 수직 배율 · 수평 배율 | array 무기: `arrMul.v` / `arrMul.h`(가상, 30발 전체에 곱함) · curve 무기: **신규 가상 배율** — 수직은 `pattern.v0·vG·vMax`에, 수평은 `pattern.h0·hMax`에 곱한다 | curve 배율은 T26.4에서 추가 (고급의 개별 값과 곱으로 합성) |
| 핸들링 | 반동 | 복귀 지연 · 복귀 속도 | `recovery.delay` · `recovery.speed` | |
| 핸들링 | 퍼짐 | base · bloom · decay · max | `spread.base` · `spread.bloom` · `spread.decay` · `spread.max` | |
| 핸들링 | 정조준 | 정조준 FOV | `config.render.adsFov` (**전역**, 무기별 아님. TBD #2) | 무기 JSON 키 없음 — 패널에는 전역 표시 |
| 핸들링 | 정조준 | 조준 시간 · 퍼짐 배율 · 반동 배율 | `ads.time` · `ads.spread` · `ads.recoil` | `ads.allowed`가 꺼지면 비활성 |
| 핸들링 | 이동 | 이동 퍼짐 배율 | `moveSpreadMul` | |
| 핸들링 | 고급(기본 접힘) | 초탄 v0 / h0 | `pattern.v0` · `pattern.h0` | curve 무기만 |
| 핸들링 | 고급 | 발당 상승 | `pattern.vG` | curve 무기만 |
| 핸들링 | 고급 | 상한 vMax / hMax | `pattern.vMax` · `pattern.hMax` | curve 무기만 |
| 핸들링 | 고급 | 수직 지터 · 수평 지터 | `randV` · `randH` | |
| 핸들링 | 고급 | 수평 방식 | `pattern.hMode` (`fixed-alt` / `random`) | curve 무기만 |
| 핸들링 | 고급 | 고정 배열 스케일 | `arrMul.v` · `arrMul.h` | array 무기만 (반동 묶음의 배율과 같은 값 — 한쪽을 바꾸면 다른 쪽도 갱신) |
| 핸들링 | 고급 | 누적 상한 | `cap.on` · `cap.deg` | |
| 핸들링 | 고급 | 웅크리기 배율 · 질주 배율 | `config.player.crouchMul` · `config.player.sprintMul` (**전역**, 이동 속도 배율) | 무기 JSON 키 없음 — 전역 표시 |
| 성장 | | (비움) "현재 성장 단계 없음" | — | T27 성장 시스템이 들어오면 채운다 |

- 기존 튜닝 패널 슬라이더 **27개**(티켓 초안의 '30개'는 잘못 센 것 — 구현 시 확인) 전부가 위 표 어딘가에 있다 (누락 없음). `falloff`(사거리 감쇠)는 기존 튜닝에도 없었으므로 이번에도 넣지 않는다 (필요하면 스펙 탭에 추가 — 사용자 결정)

**공통 동작**
- 패널을 열면 포인터 자동 해제, 닫으면 클릭으로 재잠금 (T26.2와 같은 방식)
- 파일 값과 다른 필드 옆에 **점 표시**. 탭 이름 옆에 **변경 개수 배지**
- 슬라이더 옆 숫자를 **직접 입력**할 수 있다 (Enter 확정, 범위 밖이면 잘라냄)
- 필드를 **더블클릭**하면 파일 값으로 복원
- **localStorage**: 마지막 탭, 고급 접힘 상태를 기억 (`ui.weaponPanel.v1`). ~~선택 무기~~ → **T32에서 번복: 선택 무기는 저장하지 않고 항상 CS형으로 시작** (사용자 결정 2026-09-06). 튜닝 값은 기존 키 **`tuning.v1`** 그대로 읽고 쓴다 — 저장된 값 구조를 바꾸지 않는다
- 기존 "파일 값으로 초기화", "JSON 복사" 버튼은 패널 맨 아래에 유지

**완료 조건**
- 좌상단 `무기 세팅` 버튼(T26.2 수정)으로 우측 패널이 열린다. 드롭다운으로 3정을 고를 수 있다
- 스펙·핸들링·성장 탭이 있고, 기존 튜닝 패널에 있던 슬라이더 27개가 모두 세 탭 어딘가에 있다 (위 표와 대조)
- 슬라이더를 바꾸면 사격에 즉시 반영되고, 그 필드 옆에 점이, 탭 이름 옆에 개수가 붙는다. 더블클릭하면 원래 값으로 돌아오며 점이 사라진다
- 숫자 칸에 직접 입력해도 슬라이더와 사격이 같이 바뀐다
- 새로고침해도 마지막 탭·접힘·바꾼 값이 유지된다 (선택 무기는 T32 번복으로 저장 안 함 — 항상 CS형). 이 티켓 전에 저장해 둔 `tuning.v1` 값이 그대로 읽힌다
- 성장 탭은 "현재 성장 단계 없음"만 보인다

## T29. 사운드 (합성)
**단계** 5(중간) · **의존** T26.4 · 2026-09-06 사용자 지시로 추가. **임시로 코드 합성**(음원 파일 없음 — 단일 HTML 제약). 나중에 음원 파일로 교체 가능
브라우저 오디오(Web Audio)로 소리를 만든다. 첫 클릭(포인터 잠금) 때 오디오를 켠다(브라우저 자동재생 제한).

**소리 목록** (Claude 초안, 시작 시 확인)
- 발사 — 무기마다 톤이 다르게 (CS형 짧고 단단 / PUBG형 낮고 무거움 / 데스티니형 가볍고 높음). RPM에 맞춰 발마다
- 명중 틱 / 헤드샷 틱(더 높고 짧게) / 빈 탄창 클릭 / 재장전(찰칵 2회: 시작·끝)
- 피격(둔탁, 방향 무관) / 사망(낮게 내려감) / 부활(짧은 상승)
- 웨이브 시작 카운트다운 끝(경고음) / 구역 클리어(상승 3음) / 문 열림 / 보스 등장(낮은 울림)
- 근접형 공격 준비(짧은 '휙'), 원거리형 발사(조준선 끝의 '핑')
**플레이어 세팅**에 마스터 볼륨 슬라이더 + 켜기/끄기. 볼륨은 localStorage
**완료 조건** — 쏘면 총마다 다른 발사음이 RPM에 맞춰 나고, 맞으면 틱, 머리는 다른 틱이 난다. 맞으면 피격음, 웨이브 시작·클리어·보스 등장에 소리가 난다. 플레이어 세팅에서 끄면 조용해진다

## T31. 핸들링 탭에 반동 기본값 노출 (curve 무기)
**단계** 5(중간) · **의존** T26.4 · 2026-09-06 사용자 지시로 추가
지금 핸들링 탭 반동 묶음에는 **배율**(파일값에 곱하는 값)만 있고, 배율이 곱하는 **기본값**은 고급(접힘)에 숨어 있다. curve 무기의 기본값을 반동 묶음으로 올린다. **이게 기본 형태이고, CS형(고정 배열)이 예외다.**

**할 일**
- 핸들링 탭 → 반동 묶음, curve 무기(PUBG형·데스티니형)일 때 아래 항목을 배율 위에 표시
  - 초탄 v0 (°) · 발당 상승 vG (°/발) · 상한 vMax (°) · 초탄 h0 (°) · 상한 hMax (°) · 수평 방식 (`fixed-alt` / `random`)
  - 그 아래 기존 수직 배율 · 수평 배율 (curveMul) 유지. 기본값을 개별로 바꿔 파일값×배율 관계가 깨지면 배율 칸의 '혼합' 표시는 지금대로
- 고급 묶음에서는 위 6개를 **뺀다** (같은 슬라이더가 두 곳에 나오지 않게). 고급에 남는 것: randV · randH · 고정 배열 스케일(array) · cap · 웅크리기·질주 배율
- **CS형(array)은 예외** — 반동 묶음에 수직·수평 배율만. 30발 배열 개별 편집 UI는 만들지 않는다 (사용자 결정 2026-09-06: 배열 60개 값은 파일에서 고친다)
- 점 표시·배지·더블클릭 복원·`tuning.v1` 저장은 T26.4 규칙 그대로. 저장 구조 변경 없음

**완료 조건**
- PUBG형 또는 데스티니형을 고르면 핸들링 탭 반동 묶음에 초탄 v0 / 발당 상승 vG / 상한 vMax / 초탄 h0 / 상한 hMax / 수평 방식이 보이고, 그 아래 수직·수평 배율이 있다
- v0를 올리면 첫 발부터 더 세게 튀고, 수평 방식을 바꾸면 좌우 튀는 방식이 바뀐다 (즉시 반영)
- CS형을 고르면 반동 묶음에 수직·수평 배율 두 개만 보인다
- 고급을 펼쳐도 v0·vG·vMax·h0·hMax·수평 방식은 더 이상 없다
- 새로고침해도 바꾼 값이 유지된다

## T32. 세팅 패널 슬라이더 범위 확대
**단계** 5(중간) · **의존** T31 · 2026-09-06 사용자 지시로 추가
**핸들링 탭 슬라이더의 상한을 3배, 스펙 탭 슬라이더의 상한을 10배**로 올린다. 하한과 눈금(step)은 그대로. 숫자 직접 입력의 잘라내기 범위도 같이 넓어진다.

**상한 변경표**

| 탭 | 슬라이더 | 지금 | 변경 |
|---|---|---|---|
| 스펙 | 발당 피해 | 500 | 5000 |
| 스펙 | 헤드샷 배율 | 3 | 30 |
| 스펙 | RPM | 1200 | 12000 |
| 스펙 | 탄창 | 60 | 600 |
| 스펙 | 재장전 시간 | 5 s | 50 s |
| 핸들링 | 수직·수평 배율 (arrMul · curveMul, 고급의 고정 배열 스케일 포함) | ×3 | ×9 |
| 핸들링 | 초탄 v0 / 초탄 h0 | 1.5 / 0.3 | 4.5 / 0.9 |
| 핸들링 | 발당 상승 vG | 0.1 | 0.3 |
| 핸들링 | 상한 vMax / hMax | 2 / 0.5 | 6 / 1.5 |
| 핸들링 | 복귀 지연 / 복귀 속도 | 500 ms / 40 °/s | 1500 ms / 120 °/s |
| 핸들링 | 퍼짐 base / bloom / decay / max | 2 / 0.2 / 5 / 3 | 6 / 0.6 / 15 / 9 |
| 핸들링 | 조준 시간 / 퍼짐 배율 / 반동 배율 | 600 ms / 1.5 / 1.5 | 1800 ms / 4.5 / 4.5 |
| 핸들링 | 이동 퍼짐 배율 | 8 | 24 |
| 핸들링 | 수직·수평 지터 randV · randH | 1 | 3 |
| 핸들링 | 누적 상한 cap.deg | 15° | 45° |
| 핸들링 | 웅크리기 배율 / 질주 배율 | 1.0 / 2.5 | 3.0 / 7.5 |
| 핸들링 | 정조준 FOV | 90° | **170°** (예외 — 아래) |

**예외와 동반 수정** (사용자 확인 필요한 것은 ★)
- ★ **정조준 FOV**: 3배인 270°는 카메라가 표현할 수 없다(원근 카메라 한계 180° 미만). 상한 **170°**로 제안. 다른 값을 원하면 알려달라
- **randV · randH**: 파일 검증 상한이 1이라 슬라이더만 올리면 새로고침 때 저장분이 통째로 버려진다. 검증 상한도 3으로 같이 올린다. 의미는 그대로(비율). 1을 넘으면 수직은 0 아래로 못 가니 "때때로 안 튐", 수평은 반대쪽으로 튈 수 있음
- **RPM 12000**: 발사 코드가 프레임당 한 발만 쏘므로 약 3600 RPM 위로는 화면 주사율에 막힌다. 프레임당 여러 발을 처리하도록 발사 루프를 고친다 (Claude 개발 결정). 트레이서·소리도 발수만큼 나간다
- **탄창 600**: 탄약 UI 자릿수만 확인. 재장전 50s는 그대로 기다린다
- 슬라이더가 넓어져 손으로 미세 조정이 어려운 항목은 숫자 직접 입력을 쓴다 (T26.4 기능)
- SPEC "확정" 표기 값은 **기본값**이지 범위가 아니므로 이 티켓으로 바뀌지 않는다. 웅크리기·질주 기본값(0.5 / 1.5)도 그대로

**완료 조건**
- 스펙 탭에서 발당 피해를 5000, RPM을 12000, 탄창을 600까지 끌 수 있고, RPM 12000에서 실제로 탄창 30발이 약 0.15초 안에 비워진다
- 핸들링 탭에서 수직 배율을 ×9, 복귀 속도를 120, 퍼짐 max를 9까지 끌 수 있다
- randV를 3으로 올리고 새로고침해도 값이 남아 있다 (경고 없음)
- 정조준 FOV를 170까지 끌면 화면이 그만큼 넓어진다
- 숫자 칸에 상한보다 큰 값을 넣으면 새 상한으로 잘린다

## T30. 총기 뷰모델 (단순)
**단계** 5(중간) · **의존** T29 · 2026-09-06 사용자 지시로 추가
화면 오른쪽 아래에 그레이박스 총(박스 2~3개). **총마다 모양 또는 색이 다르게 — 구분만 되면 된다** (색은 탄착 색과 같게).
- 발사 시 뒤로 밀렸다 돌아옴(반동 크기에 비례), 정조준 시 화면 중앙으로 이동(ads 진행도에 따라), 재장전 시 아래로 내려갔다 올라옴, 무기 전환 시 내려갔다 새 총이 올라옴
- 카메라 앞 고정(별도 씬 또는 카메라 자식) — 벽을 뚫고 보여도 됨(그레이박스)
- 플레이어 세팅에 켜기/끄기
**완료 조건** — 화면 오른쪽 아래에 총이 보이고 무기를 바꾸면 모양·색이 바뀐다. 쏘면 총이 튀고, 우클릭하면 중앙으로 들어온다. 설정에서 끌 수 있다

## T33. 구역별 지형 차별화
**단계** 5(중간) · **의존** T26 · 2026-09-06 사용자 지시로 추가
지금 구역 1·2·3은 같은 30×30 방에 낮은 상자·높은 벽을 자리만 바꿔 놓아 **너무 같은 느낌**이다. 구역마다 엄폐물 배치의 **성격**이 다르게 보이도록 다시 짠다. 배치 수치는 Claude 임시(CLAUDE.md 3-2), **구역별 컨셉은 시작 시 사용자 확인**.

**컨셉 초안** (Claude 제안. 사용자 지시 2026-09-06: 구역별 구성은 **T33 시작 시 다시 생각한다** — 아래 표는 출발점일 뿐 확정 아님)
| 구역 | 컨셉 | 엄폐물 성격 |
|---|---|---|
| 1 | **열린 마당** | 낮은 상자만 드문드문. 높은 벽 없음. 시야가 넓어 원거리형과 정면 대결 — 반동 제어가 그대로 드러남 |
| 2 | **갈림길** | 방 가운데를 긴 높은 벽으로 갈라 좌·우 두 통로. 근접형이 옆 통로로 돌아 들어옴. 모퉁이 교전 |
| 3 | **중앙 요새** | 방 가운데 높은 벽 뭉치(ㄷ자) + 그 주변 낮은 상자 고리. 안에 들어가면 사방 엄폐, 밖에서는 우회 강요 |

- 방 크기(`halfWidth`, 깊이)도 구역마다 달라도 된다 (`ROOMS`가 방별 값을 이미 지원). 예: 구역 2는 좁고 길게, 구역 3은 넓게
- 몬스터 단(2m, 원거리형 전용)·문 위치·웨이브 구성은 이 티켓에서 바꾸지 않는다 (문·통로는 T34)
- 스폰 좌표(`makeWaves`의 근접형 x −6~6, 단 위 원거리형)가 새 엄폐물과 겹치지 않게 조정
- 보스 방은 그대로

**완료 조건**
- 구역 1·2·3에 들어섰을 때 세 방이 **한눈에 다른 방**으로 보인다 (열린 마당 / 갈림길 / 중앙 요새)
- 각 방에서 몬스터가 엄폐물 속에 끼거나 벽 안에 스폰되지 않는다
- 사격장·보스 방·문·웨이브는 이전과 같이 동작한다

## T34. 클리어 후 다음 구역 안내
**단계** 5(중간) · **의존** T26 · 2026-09-06 사용자 지시로 추가
구역을 클리어하면 뒷벽 문이 열리지만 **문이 몬스터 단 뒤에 가려져 있어** 어디로 가야 할지 알기 어렵다. 단과 뒷벽 사이 통로도 1m라 좁다. 클리어 즉시 **직관적으로 다음 구역 방향을 알 수 있게** 한다.

**방법 후보와 개발 비용** (사용자가 "바닥에 선"을 제안 — 비용 확인 요청)
| 방법 | 내용 | 비용 |
|---|---|---|
| A. 바닥 유도선 | 클리어 시 방 중앙 → 단 옆 우회 → 문까지 **바닥 화살표/점선**이 나타남. 문을 지나면 사라짐 | **약 1~2시간**. `range.js`의 floorLine은 가로선만 그려서 "위치·방향·길이"를 받는 헬퍼 하나 추가 + 클리어 시 표시/리셋 시 숨김 |
| B. 문 강조 | 열린 문 자리가 **초록 빛기둥**(벽·단 너머로도 보이는 세로 기둥) + 문틀 색 변경 | 약 30분~1시간 |
| C. 통로 넓힘 | 단과 뒷벽 사이 1m → **3m**, 또는 단을 앞으로 당김. 단 옆으로 돌아가는 길이 눈에 보이게 | 약 30분 (`ROOMS.platform` z 값 조정) |
| D. 문을 단 옆으로 | 문을 뒷벽 가운데가 아니라 좌 또는 우로 옮겨 단에 가려지지 않게 | 약 1시간 (문 위치 파라미터화, 유도 텍스트 위치 같이) |

**사용자 결정(2026-09-06): A + B + C 적용** (합계 약 3시간). D는 하지 않는다. A만으로는 문이 안 보이는 문제(B)와 좁은 통로(C)가 남기 때문.

- 클리어 문구("구역 N 클리어 — 앞으로 가세요")는 유지, 필요하면 "→ 단 옆으로 돌아가세요"로 보강
- 스테이지 리셋(M)에서 유도선·빛기둥이 같이 사라진다
- 보스 방은 클리어 = 데모 완료라 유도 불필요

**완료 조건**
- 구역을 클리어하면 **1초 안에** 화면에서 다음 구역 가는 길이 보인다 (선택한 방법대로)
- 처음 플레이하는 사람이 클리어 후 문을 찾아 헤매지 않고 다음 구역으로 간다 (사용자가 직접 확인)
- 문을 지나면 안내가 사라지고, M 리셋 후 다시 클리어하면 다시 나타난다

# 반동 재조정 + 강화 시스템 (2026-09-07 사용자 설계 — T27 대체)

사용자가 정리한 밸런스 설계(2026-09-07)를 티켓 6개로 나눴다. 흐름은 **① 반동을 실제 CS·PUBG 수준으로 키운다(시각 추적 분리, 지향 FOV 75) → ② 사격장에서 명중률을 재서 발당 피해를 역산한다 → ③ 구역 클리어마다 무기 3종 각각 강화 카드를 한 장씩 고르는 성장을 넣는다**. 기존 T27(드랍과 파워 상승)은 T38~T40이 대체한다.

**순서(확정 2026-09-07, 사용자가 Claude 판단에 위임)**: T35 → T36 → T37 → T33 → T34 → T38 → T39 → T40 → T28. 반동과 발당 피해가 먼저 확정되어야 강화 카드에 찍히는 TTK 숫자가 맞는다.

**기준 수치 (사용자 설계 2026-09-07 — T37 측정 뒤 발당 피해만 다시 역산)**

| 무기 | RPM | 발당 피해 | 헤드 배율 | 가정 명중 / 헤드율 | 기대 DPS | 3,000 HP TTK | 몸샷 / 헤드 처치 |
|---|---|---|---|---|---|---|---|
| CS형 | 600 | 170 | 2.0 | 0.55 / 0.40 | 1,309 | 2.29 s | 18 / 9발 |
| PUBG형 | 600 | 190 | 1.5 | 0.60 / 0.30 | 1,311 | 2.29 s | 16 / 11발 |
| 루트슈터형 | 660 | 120 | 1.5 | 0.85 / 0.30 | 1,290 | 2.33 s | 25 / 17발 |

기대 DPS = RPM/60 × 명중 × (1 − 헤드율 + 헤드율 × 헤드 배율) × 발당 피해. **세 무기 기대 DPS 동일 원칙**(≈1,304, TTK 2.3 s). 거리 감쇠는 20m까지 1.0이라 표에 영향 없음.

## T35. 반동 시각 추적(viewTracking)과 지향 FOV 75
**단계** 5(중간) · **의존** T32 · 2026-09-07 사용자 설계
CS형을 실제 CS 크기로 키우려면 "탄은 25° 올라가는데 화면은 11°만 올라간다"를 먼저 만들어야 한다. 값 교체(T36)보다 앞선 **룰 추가** 티켓.

**할 일**
- 신규 무기 필드 **`viewTracking`** (0~1, 기본 1.0 = 지금과 동일). 누적 반동 중 **카메라가 따라가는 비율**. 탄은 항상 누적 반동 100%만큼 올라가고, 카메라는 그중 viewTracking만큼만 올라간다. 0.45면 카메라가 11° 올라갈 때 탄은 25° 위로 간다 → "화면은 덜 흔들리는데 안 잡으면 탄이 천장으로 간다"(CS 감각)
  - 복귀(recovery)는 카메라분과 숨은 분을 같이 되돌린다. 보정분 제외 규칙 그대로
  - 이론 궤적 오버레이(P)와 탄착군은 **탄이 실제로 가는 자리**(숨은 반동 포함) 기준. 조준원은 카메라 중앙 그대로
  - cap은 누적 반동 전체(탄도 기준)에 건다. 뷰모델(T30) 반동 연출은 카메라 기준(viewTracking 곱한 값)
  - 무기 세팅 패널 핸들링 탭 반동 묶음에 `viewTracking` 슬라이더(0~1, step 0.05). 점·배지·더블클릭·`tuning.v1` 저장은 T26.4 규칙. 스키마 검증(범위 밖·빈칸 → 경고 배지 + 기본값 1)
- 지향 FOV **90 → 75** (`config.render.fov`, 플레이어 세팅 슬라이더 기본값도 75). 같은 각도가 지금 FOV에서는 25% 작아 보여 반동 체감이 덜 산다. SPEC 3장 "확정 90"의 변경 — 사용자 설계에 명시되어 있으므로 승인된 것으로 보고, 단계 종료 시 SPEC 갱신. 조준원 반지름은 FOV를 이미 반영하므로 저절로 커진다
- 정조준 FOV를 **무기별 필드 `ads.fov`**로 (신규 선택 필드. 없으면 전역 `config.render.adsFov` 59). 무기 세팅 패널 정조준 묶음의 "정조준 FOV"는 전역 대신 무기별 값을 편집하고, 전역 슬라이더는 플레이어 세팅에만 남긴다. T36에서 PUBG형 55 / 루트슈터형 65가 들어간다. TBD #2는 이걸로 종결
- 사용자 설계의 CS형 `crouchMul 0.7`은 **웅크리기 퍼짐 배율** — 신규 무기 필드 `crouchSpreadMul`(기본 1.0, 웅크린 동안 퍼짐에 곱함). 사용자 확인 2026-09-07

**완료 조건**
- CS형을 들고 무기 세팅 → 핸들링 → 반동에서 viewTracking을 0.45로 내리고 벽에 30발을 쏘면, 화면은 전보다 절반쯤만 올라가는데 탄착군과 P 이론 궤적은 예전 높이만큼 올라간다. 1.0으로 되돌리면 지금과 같다
- 0.45에서 사격을 멈추면 카메라와 이론 궤적 시작점이 함께 원위치로 돌아온다
- 게임을 열면 시야가 지금보다 좁게 보인다(75). 플레이어 세팅에서 90으로 올리면 예전 화면이다
- 무기 세팅 정조준 묶음에서 PUBG형의 정조준 FOV를 바꾸면 PUBG형 정조준만 바뀌고 루트슈터형은 그대로다
- CS형에서 C로 웅크리면 조준원이 작아진다 (crouchSpreadMul)

## T36. 3정 값 교체 — CS형 ×5 고정 배열 / PUBG형 AKM 기준 / 루트슈터형 유지
**단계** 5(중간) · **의존** T35 · 2026-09-07 사용자 설계
`data/weapons.json` 값을 아래로 바꾼다. **룰(코드)은 건드리지 않는다.** 시뮬레이터 원본 값은 git 히스토리에 남는다. 전부 탄도 기준(°)이고 화면에 얼마나 보이는지는 viewTracking이 정한다. 사격장에서 확인한 뒤 조정하는 출발점.

**CS형 (`cs`) — 고정 배열, 탄도 25° / 화면 11°**

| 필드 | 값 | 이유 |
|---|---|---|
| `rpm` / `damage` / `headshotMul` | 600 / **170** / **2.0** | 기준 수치 표 |
| `pattern.arr` (수직, 수평) 30발 | `[1.1,0] [2.2,0.1] [3.2,-0.1] [4.0,0.2] [4.4,0.2] [4.2,0.3] [3.6,0.3] [1.6,0.4] [0.8,0.5] [0.5,0.8]` `[0.3,1.3] [0.3,1.5] [0.25,1.5] [0.25,1.4] [0.2,1.2] [0.2,1.0] [0.2,0.6] [0.2,-0.8] [0.2,-1.5] [0.2,-1.7]` `[0.2,-1.7] [0.2,-1.5] [0.2,-1.2] [0.2,-0.8] [0.2,0.6] [0.2,1.0] [0.2,1.2] [0.2,1.2] [0.2,1.0] [0.2,0.8]` | 10발 누적 수직 25.6°, 30발 29.9°. 수평은 10발 이후 우로 11°까지 갔다가 좌로 되돌아오고 다시 우로 — AK의 "7자 → 좌 → 우". 현재의 ×5 |
| `viewTracking` | 0.45 | 화면 11°, 탄 25° |
| `randV` / `randH` | 0.05 / 0.05 | 패턴은 고정, 미세 흔들림만 |
| `spread` base / bloom / decay / max | 0.15 / 0.12 / 6.0 / 1.2 | 초탄 정확, 연사할수록 벌어지고 멈추면 빠르게 회복 |
| `moveSpreadMul` / `crouchSpreadMul` | 4.0 / 0.7 | 이동 사격 불가 수준. 브레이킹이 의미를 가지려면 필요 |
| `recovery` delay / speed | 100 ms / 40 °/s | 사격 중단 후 0.7초 안에 완전 복귀 |
| `ads.allowed` / `cap.on` | false / false | |

**PUBG형 (`pubg`) — 곡선, AKM 기준 34°**

| 필드 | 값 | 이유 |
|---|---|---|
| `rpm` / `damage` / `headshotMul` | **600**(660→) / **190** / 1.5 | |
| `pattern` v0 / vG / vMax | 0.72 / 0.052 / 1.24 | 10발 9.5°, 30발 34.3°. 현재의 ×2 |
| `pattern` hMode / hMax | random / 1.1 | 좌우 무작위. AKM 수평 흔들림 (`h0`는 random 모드에서 안 쓰므로 유지) |
| `randV` / `randH` | 0.15 / 0.30 | 수직은 예측 가능, 수평은 불규칙 |
| `viewTracking` | 1.0 | 카메라와 탄도 일치 |
| `spread` base / bloom / decay / max | 1.2 / 0.05 / 3.0 / 2.0 | 지향 사격 부정확. 정조준을 강제 |
| `ads` spread / recoil / fov / time | 0.15 / 1.0 / 55 / 250 ms | 정조준하면 퍼짐만 줄고 반동은 그대로 |
| `moveSpreadMul` | 2.0 | |
| `recovery` delay / speed | 150 ms / 10 °/s | 느린 복귀. 긁는 동안 누적 유지 |
| `cap` | off (`deg`는 개머리판 강화가 씀 — T38) | |

M416 느낌으로 낮추고 싶으면 v0 0.55 / vG 0.04 / vMax 0.95 (30발 약 26°).

**루트슈터형 (`d2`, 현재 이름 "데스티니형 AR") — 곡선, 최약 8.6°**

| 필드 | 값 | 이유 |
|---|---|---|
| `name` | **"루트슈터형 AR"** (id `d2`·색·태그는 유지) | 개명 — 사용자 확인 2026-09-07. 조작키 표·카드·문서의 이름 참조도 같이 |
| `rpm` / `damage` / `headshotMul` | **660**(600→) / **120** / 1.5 | |
| `pattern` v0 / vG / vMax | 0.22 / 0.008 / 0.30 | 현재 유지. 10발 2.6°, 30발 8.6° |
| `pattern` hMode / hMax | **random**(fixed-alt→) / 0.25 | |
| `randV` / `randH` | 0.10 / 0.20 | |
| `viewTracking` | 1.0 | |
| `spread` base / bloom / decay / max | 0.30 / 0.02 / 8.0 / 0.8 | 지향도 무난, 벌어짐 거의 없음 |
| `ads` spread / fov / time | 0.5 / 65 / 180 ms (`recoil`은 설계에 없어 0.80 유지) | 정조준 이득이 작아 지향 위주 플레이 허용 |
| `moveSpreadMul` | 1.3 | |
| `recovery` delay / speed | 50 ms / 25 °/s | 즉시 리센터 |
| `mag` / `cap` | 33 유지 / off | |

**동반 작업**
- 파일 값이 바뀌면 옛 `tuning.v1` 저장분이 새 값을 덮어쓴다 → 저장분에 파일 값 지문을 넣고 다르면 그 무기 저장분을 버린다 (패널 상단에 "파일 값이 바뀌어 저장된 튜닝을 초기화했습니다" 한 줄). Claude 개발 결정
- 무기 카드 `desc` 문장은 유지 (PUBG형 "복귀가 느려 끊어 쏘는 총"은 여전히 맞다). 이름 변경분만 반영
- 사격장 표적 HP 3000, 몬스터 HP는 아직 그대로. docs 비교표 갱신은 T37(피해 확정 뒤)
- 세 무기가 갈리는 지점: CS형은 반동이 크지만 고정이고 화면에는 절반만 보이며 이동하면 못 쏜다. PUBG형은 반동이 가장 크고 수평이 무작위이며 지향 사격이 안 되고 정조준하면 퍼짐만 잡힌다. 루트슈터형은 반동이 거의 없고 어디서든 쏜다. **무기 세팅 패널에서 세 무기를 나란히 놓고 숫자만으로 읽혀야 한다**

**완료 조건**
- CS형: 벽에 30발 연사하면 첫 10발 사이에 탄착이 크게(화면 절반 높이쯤) 올라가고 그 뒤 우→좌→우로 꺾인다. P 궤적이 그 모양이다. 마우스로 끌어내리며 쏘면 탄착이 한 자리에 모인다. 걸으면서 쏘면 조준원이 크게 벌어진다
- PUBG형: 지향으로 쏘면 조준원이 커서 25m 표적을 맞추기 어렵고, 정조준하면 조준원은 작아지지만 30발 연사에 카메라가 화면 상단 근처까지 올라간다. 좌우는 매번 다르게 튄다
- 루트슈터형: 어디서 쏴도 30발이 표적 근처에 모이고, 손을 떼면 바로 원위치다
- 무기 세팅 스펙 탭에 170 / 2.0 / 600, 190 / 1.5 / 600, 120 / 1.5 / 660이 보인다. 라인업 이름이 루트슈터형이다 — **T37 역산 뒤 발당 피해는 190 / 190 / 140** (2026-09-07, TTK 2.0 s 기준)
- 사격장 25m 인간형 표적(HP 3000)이 CS형 몸샷 18발 / 헤드 9발에 쓰러진다
- 이전에 저장된 튜닝 값이 있어도 새 파일 값으로 시작하고 안내 한 줄이 보인다

## T37. 사격장 명중률 측정과 발당 피해 역산
**단계** 5(중간) · **의존** T36 · **사용자 측정이 들어가는 티켓**
기준 수치 표의 명중률(0.55 / 0.60 / 0.85)은 옛 반동 기준 가정이다. T36 뒤 사격장에서 실제 명중률을 재고, 그 값으로 발당 피해를 다시 역산한다. 순서: 반동 확정 → 명중률 측정 → 피해 갱신.

**할 일**
- **명중률 카운터**: 사격장 표적(인간형 3개)에 맞은 것만 센다. 무기별 발사 / 명중 / 헤드 / 명중률 / 헤드율. 플레이어 세팅(디버그) "명중률" 그룹, 초기화 버튼 (X 탄착군 지우기와 같이 초기화). 몬스터·과녁판·벽은 세지 않는다
- 측정 절차(사용자): 25m 인간형 표적에 무기마다 **10발 점사 × 5회 이상**. CS형은 지향 + 끌어내리기, PUBG형은 정조준, 루트슈터형은 지향. 명중률을 Claude에게 알려준다
- Claude가 역산: 발당 피해 = 3,000 ÷ (2.3 × RPM/60 × 명중 × (1 − 헤드율 + 헤드율 × 헤드 배율)), 10 단위 반올림. 세 무기 기대 DPS 오차 ±2% 안. `weapons.json` 갱신, 결과를 이 티켓 표에 기록
- 헤드율은 측정값이 아니라 **설계 가정(0.40 / 0.30 / 0.30)을 유지**한다 (측정 때 머리를 노리지 않으므로). 사용자 확인 2026-09-07
- `docs/총기별 세팅 비교표.md` 표 A(weapons.json 대조)를 새 값으로, `docs/총기 감각 구성요소 분해.md`의 반동 크기 서술(CS형 30발 5.8° 등)을 새 크기로 갱신. SPEC 갱신은 단계 종료 시

**완료 조건**
- 사격장에서 10발 쏘면 플레이어 세팅 명중률 그룹의 발사가 10, 명중이 맞은 수만큼 오른다. 무기를 바꾸면 그 무기 줄이 따로 센다. 초기화로 0
- 구역의 몬스터를 쏴도 카운터가 움직이지 않는다
- 역산 뒤 무기 세팅 스펙 탭의 발당 피해가 새 값이고, 25m 인간형 표적이 몸샷 N발에 쓰러지는 수가 갱신된 표와 맞는다
- 비교표 문서의 숫자가 weapons.json과 같다

## T38. 강화 데이터와 적용 (T27 대체 ①)
**단계** 5 · **의존** T37
무기 3종 × 강화 3장, 각 Lv 0~3. **핵심/보조 구분 없음** (사용자 결정 2026-09-07 — 카드는 사용자 CSV로 확정, 초안 폐기). 강화는 **각 무기에 따로** 붙고 무기를 바꿔 들어도 유지된다. 이 티켓은 데이터 · 적용 로직 · 성장 탭 · 정조준 이동 페널티까지. 선택 화면은 T39.

**강화 카드 9장 (사용자 확정 2026-09-07, CSV)**

| 무기 | 이름 | Lv1 | Lv2 | Lv3 | 구현 메모 (Claude) |
|---|---|---|---|---|---|
| CS형 | 헤드헌터 | 헤드샷 피해 350% | 500% + 헤드샷 시 탄약 1발 환급 | 700% + 헤드샷 시 탄약 2발 환급 | 탄약 환급판 채택 (사용자: "가능하면" → 가능). `headshotMul` 2.0 → 3.5 / 5.0 / 7.0. 환급은 현재 탄창에 더하고 탄창 상한까지, 재장전 중엔 없음 |
| CS형 | 브레이킹 | 정지 시 첫 3발 퍼짐 0 | 정지 시 첫 5발 퍼짐 0 | 정지 시 첫 5발 퍼짐 0 + 퍼짐 전체 ×0.5 | "정지" = 이동 속도 0.1 m/s 미만. "첫 N발" = 패턴 인덱스 기준(사격 정지 500 ms 후 리셋). 그동안 조준원도 최소 크기로 |
| CS형 | 멀티킬 | 처치 시 퍼짐(bloom) 초기화 | + 5초간 피해 +25% | + 5초간 피해 +100% | "처치" = 몬스터 사망 + **사격장 인간형 표적 쓰러뜨림**(검증용). 버프는 재처치 시 시간 갱신(중첩 없음). 무기 슬롯에 남은 시간 표시 |
| PUBG형 | 반동 제어 | 수직 −15% | 수직 −30% · 수평 −10% | 수직 −50% · 수평 −33% | `v0·vG·vMax` ×0.85 / 0.70 / 0.50, `hMax` ×1 / 0.90 / 0.67. 튜닝값과 곱, 바닥값 40% |
| PUBG형 | 탄창 개조 | 장탄수 +40% | +40% · 재장전 −33% | +40% · 재장전 −50% | 30 → 42발(반올림). 현재 탄은 비율 유지. 재장전 2.0 → 1.34 / 1.0 s |
| PUBG형 | 조준 보정 | 조준 속도 +40% | + 총구 흔들림 −33% | + 정조준 이동 페널티 없음 | `ads.time` ÷1.4 (250 → 179 ms). 흔들림 = 정조준 중 뷰모델 반동 연출 ×0.67 (수치 임시, 사용자 요청대로 조정). Lv3: 정조준 이동 ×0.6 → ×1.0 |
| 루트슈터형 | 속사 | 연사 +10% | +33% | +50% | `rpm` ×1.10 / 1.33 / 1.50 (660 → 726 / 878 / 990) |
| 루트슈터형 | 강화탄 사격 | 5발마다 2배 강화탄 | 3발마다 | 2발마다 | N발째마다 피해 ×2. 카운터는 무기별로 탄창·재장전과 무관하게 이어 센다. 강화탄은 트레이서 굵고 밝게 + 피해 숫자 강조 |
| 루트슈터형 | 장탄수 회복 | 처치 시 탄 +10 | 처치 시 탄창 가득 | 처치 시 10초간 탄약 소모 없음 | 탄창 상한까지. Lv3 버프는 갱신, 슬롯에 남은 시간 |

- 카드에 **기대 TTK는 표시하지 않는다** — 효과가 이질적이라 하나의 숫자로 못 만든다 (핵심/보조 폐지에 따라). 효과 한 줄만
- **정조준 이동 페널티 신설** (사용자 결정 2026-09-07): 모든 무기, 정조준 중 걷기 속도 **×0.6** (`config.player.adsMoveMul`, Claude 임시). 정조준 진행도(ease)에 비례해 보간. 질주 불가는 지금대로. PUBG형 조준 보정 Lv3에서만 ×1.0
- **성장 반영 TTK 표(옛 핵심/보조 기준)는 폐기.** 몬스터 HP 구역 배율 1.0 / 1.1 / 1.2는 유지

**할 일**
- 강화 데이터는 **코드**에 둔다 (`src/growth/cards.js` — 9장 정의, `src/growth/growth.js` — 무기별 Lv 상태·최종값 계산·버프 타이머. CLAUDE.md 3-1: 파일은 무기만). 지원 필드 목록 밖의 효과는 **경고 배지 + 그 강화만 무시**
- 무기별 상태 { 카드 3장 Lv } + 선택 횟수. 수치 효과(헤드샷 배율 · 반동 · 탄창 · 재장전 · 조준 시간 · RPM)는 **최종값 = 튜닝값 × 강화 배율** (원본·튜닝·강화·최종 네 층). 튜닝 슬라이더·파일 값 초기화·JSON 복사는 튜닝 층만 다루고 강화는 독립
- 행동 효과(헤드샷 환급 · 브레이킹 · 멀티킬 · 강화탄 · 장탄수 회복 · 조준 보정 흔들림/이동)는 shooter · spread · movement · viewModel이 무기의 `growth`를 읽어 처리. 처치 이벤트(`kill`)는 monsters/targets가 쏘고 main이 장착 무기의 growth로 넘긴다
- **반동 감소 바닥값**: 반동 배율의 최종값이 원본의 40% 아래로 못 내려간다 (튜닝으로 이미 낮췄어도)
- 반동 변경은 재컴파일, RPM·피해·탄창·재장전은 즉시 (탄창이 늘면 현재 탄도 비율 유지)
- 무기 세팅 **성장 탭**: 무기 3종 각각 카드 3장의 현재 Lv + 효과 한 줄, 수치 효과는 **원본 · 튜닝 · 강화 · 최종** 표. **개발 확인용 Lv +/− 버튼** (T39 전까지 유일한 조작 수단, 그 뒤에도 디버그로 남긴다). "현재 성장 단계 없음" 제거
- 정조준 이동 페널티 (`movement.js`, 전역 ×0.6, 플레이어 세팅 슬라이더)
- **몬스터 HP 구역 배율 1.0 / 1.1 / 1.2** (구역 1·2·3, 근접·원거리 모두. 사용자 설계). 보스 20,000 유지, 보스 방 소환 근접형은 1.2 (Claude 임시). 플레이어 세팅 HP 슬라이더는 기본 HP, 배율은 그 위에 곱

**완료 조건**
- 무기 세팅 → 성장 탭에 무기 3종 각각 카드 3장(이름·Lv·효과 한 줄)과 수치 효과 표(원본·튜닝·강화·최종)가 보인다
- CS형 헤드헌터 Lv1: 스펙 탭 헤드샷 배율은 2.0 그대로, 성장 탭 최종은 3.5, 사격장 헤드샷 피해 숫자가 665(190 × 3.5). Lv2에서 헤드샷을 맞히면 슬롯의 탄 수가 1 오른다
- CS형 브레이킹 Lv1: 서서 쏘면 첫 3발은 조준원이 점이고 4발째부터 원이 생긴다. 걸으면서 쏘면 첫 발부터 원이다
- CS형 멀티킬 Lv2: 표적을 쓰러뜨리면 슬롯에 5초 타이머가 뜨고 그동안 피해 숫자가 238(190 × 1.25)이다
- PUBG형 반동 제어 Lv3: 30발 연사 상승이 34°에서 17°로 준다 (P 궤적). 탄창 개조 Lv1: 슬롯이 42발. 조준 보정 Lv1: 우클릭 조준이 눈에 띄게 빨라진다
- 모든 무기: 정조준한 채 걸으면 느려진다. PUBG형 조준 보정 Lv3에서만 정조준해도 걷기 속도 그대로다
- 루트슈터형 속사 Lv3: 스펙 탭 RPM은 660, 성장 탭 최종은 990, 한 탄창이 2초에 빈다. 강화탄 사격 Lv1: 5발째마다 굵은 트레이서와 2배 피해 숫자. 장탄수 회복 Lv3: 표적을 쓰러뜨린 뒤 10초간 탄이 안 줄어든다
- 튜닝 슬라이더로 값을 바꾸면 최종 열이 따라 바뀌고 Lv는 유지된다
- 구역 2·3의 근접형이 구역 1보다 더 많이 맞아야 죽는다 (3,000 → 3,300 → 3,600) — 보스 소환수는 배율 1.0(사용자 결정). **T46에서 구역 배율을 제거하고 일반·정예 티어로 바꾼다**

## T39. 구역 클리어 강화 선택 화면 (T27 대체 ②)
**단계** 5 · **의존** T38
구역 1·2·3 클리어 시, 문이 열리기 전에 선택 화면이 뜬다. **총 9회 선택, 무기당 3회.** 보스 클리어 후에는 없다.

**화면**
- 게임 정지(몬스터·타이머·입력 모두), 포인터 해제. 한 화면에 **세 줄**, 위부터 **CS형 · PUBG형 · 루트슈터형** 고정. 각 줄에 그 무기의 강화 3장이 CSV 순 (헤드헌터 · 브레이킹 · 멀티킬 / 반동 제어 · 탄창 개조 · 조준 보정 / 속사 · 강화탄 사격 · 장탄수 회복). 핵심/보조 구분 없음 (순차 팝업이 아니라 세 줄 나란히 — 리뷰어가 "세 무기가 다른 방식으로 자란다"를 한눈에 보게)
- 카드: 이름 · 현재 → 다음 Lv · **다음 Lv의 효과 한 줄** (T38 표 문구). 기대 TTK는 표시하지 않는다 (사용자 결정 2026-09-07). Lv3인 카드는 회색 "최대"로 선택 불가. 한 줄에 선택 가능한 카드가 하나뿐이면 자동 선택되어 표시
- 줄마다 한 장을 고르고, **세 줄이 다 골라지면 "확정" 활성**. 확정 전에는 어느 줄이든 바꿀 수 있고, 확정 후 되돌리기 없음
- 확정 → 화면 닫힘 → 문 열림 + T34 안내 + 클리어 프롬프트 → 클릭으로 포인터 재잠금. 확정 시 T29 클리어 음 재사용(새 소리 없음)

**예외 처리 (사용자 설계)**

| 상황 | 정책 |
|---|---|
| 클리어와 동시에 사망 | 클리어 판정 우선. 선택 화면 → 닫힌 뒤 부활 처리 |
| 선택 화면에서 ESC / Tab | 무시. 세 줄 확정 전에는 닫히지 않고 일시정지 메뉴도 안 열린다 |
| 일부만 고르고 확정 시도 | 확정 비활성. 미선택 줄 강조 |
| 선택 화면 중 무기 전환 키·휠 | 무시. 닫힌 뒤 마지막 무기로 복귀 |
| 사망 후 부활 | 강화 유지 |

**완료 조건**
- 구역 1 웨이브를 모두 처치하면 즉시 화면이 멈추고 마우스가 풀리며 3줄 × 3장 선택 화면이 뜬다. 문은 아직 닫혀 있다
- 각 줄에서 한 장씩 고르기 전에는 확정이 눌리지 않고, 두 줄만 고르고 누르면 남은 줄이 강조된다
- 카드에 이름 · 현재 → 다음 Lv · 다음 Lv 효과 한 줄이 있고, 문구가 T38 표와 같다
- 확정하면 화면이 닫히고 문이 열리며, 클릭하면 바로 조작된다. 무기 세팅 성장 탭에 방금 고른 Lv가 반영되어 있다
- 선택 화면에서 ESC · Tab · 숫자키 · 휠은 아무 일도 하지 않는다
- 구역 3까지 세 번 고르면 각 무기가 3-0-0 / 2-1-0 / 1-1-1 중 하나이고, 3-0-0이면 그 카드가 회색 "최대"다. 보스 처치 후에는 선택 화면이 뜨지 않는다
- 마지막 몬스터의 공격에 HP 0이 되며 클리어해도 선택 화면이 먼저 뜨고, 닫힌 뒤 부활한다

## T40. 강화 저장 · 리셋 · 튜닝 연동 (T27 대체 ③) — **제외** (사용자 결정 2026-09-07)
**단계** 5 · **의존** T39 · 아래는 원래 계획. 강화는 저장하지 않는다(새로고침 시 Lv 0, M 리셋도 강화는 그대로). T28은 T39 뒤에 바로 진행.

**할 일**
- localStorage `growth.v1`: 무기 3종 Lv + 선택 횟수. 새로고침 시 복원. 스테이지 진행은 저장하지 않으므로(사격장에서 시작) **사격장에서 강화된 세 무기를 비교**할 수 있다
- 복원 뒤 구역을 다시 클리어할 때 선택 화면은 **구역 번호 > 선택 횟수일 때만** 뜬다 (Claude 결정 — 무기당 3회 상한을 지키기 위해). 구역 1을 다시 깨면 화면 없이 문만 열린다
- **스테이지 리셋(M)**: 세 무기 강화 전부 초기화, 선택 횟수 0, 저장분 삭제
- 사격장 복귀(걸어서): 강화 유지
- 튜닝 원본 값 변경 / 파일 값 초기화: 강화 유지, 최종값만 재계산 (T38 로직을 여기서 검증). JSON 복사는 튜닝값(강화 제외)
- 바닥값 40% 도달: 클램프. 클램프에 걸리는 등급은 카드 효과 줄에 "(바닥값)" 표시
- 강화 데이터 미지원 필드: 경고 배지 + 해당 강화만 무시 (T38 로직 검증)
- T28 결과 화면의 "획득한 장비 목록"은 **무기별 강화 Lv**로 (T28에서 반영)

**완료 조건**
- 구역 1 클리어 후 강화를 고르고 새로고침하면 사격장에서 시작하지만 성장 탭 Lv가 남아 있고, 사격장에서 세 무기가 강화된 상태로 쏴진다
- 그 상태로 구역 1을 다시 클리어하면 선택 화면 없이 문만 열리고, 구역 2 클리어에서 두 번째 선택이 뜬다
- M을 누르면 Lv가 전부 0이고 새로고침해도 0이다
- 무기 세팅에서 파일 값으로 초기화를 눌러도 Lv가 남고 최종 열만 다시 계산된다
- 튜닝 수직 배율을 0.5로 내리고 컴펜세이터 Lv3이면 최종 배율이 0.25가 아니라 0.4다 (성장 탭 최종 열)
- 강화 데이터에 없는 필드를 일부러 넣으면 우상단 경고 배지가 뜨고 그 강화만 빠진다

---

## T27. 드랍과 파워 상승 — **T38~T40으로 대체** (2026-09-07)
**단계** 5 · **의존** T26 · 아래는 원래 계획. 성장은 '드랍·해금' 대신 **구역 클리어마다 무기 3종 강화 카드 선택**으로 바뀌었다 (TBD #11 해금 → 강화 카드로 대체, 단계 종료 시 SPEC 반영)
구역 클리어 시 **같은 총의 피해량·탄창 상승 + 기능 해금**. 최소 인벤토리 화면. 해금 내용은 시작 시 인터뷰 (SPEC TBD #11)
**완료 조건** — 구역을 클리어하면 보상을 받고, 인벤토리에서 총의 수치가 오른 것이 보인다. 다음 구역에서 총이 실제로 세졌다고 느껴진다

## T28. 결과 화면 — **완료** (2026-09-07, `7d809f4`)
**단계** 5 · **의존** T39 (T40 제외로 변경)
클리어 시간 / 명중률·헤드샷 비율 / 총기별 사용 비율 / 획득한 장비 목록(→ **무기별 강화 Lv**)

**인터뷰 확정 (2026-09-07)**
- 클리어 시간: **구역 1에 들어선 순간 → 보스 처치**. 강화 선택·결과 화면으로 멈춘 시간은 제외 (게임 시각)
- 총기별 사용 비율: **발사 수 비율 + 피해량 비율** 둘 다. 무기별 명중률도 한 열
- '처음부터 다시 시작': **강화 Lv 0 + 사격장 시작 위치 + 스테이지·HP·통계 리셋**. 튜닝(무기 세팅)은 유지. 장착 무기는 CS형, 탄창 가득
- 등장: **보스 처치 2초 뒤**. 버튼은 '처음부터 다시 시작' 하나. 뜨는 동안 게임 정지·포인터 해제·일시정지 메뉴 차단(T39와 같은 방식)
- Claude 결정: 명중률 분모 = 타이머가 도는 동안 쏜 모든 발, 분자 = 몬스터 몸·머리 명중(피해 > 0). 헤드샷 비율 = 헤드 / 명중. 피해량 = 실제 입힌 값(배율 포함). M 리셋은 결과·통계도 비움(강화는 그대로)

**코드**: `src/stats/runStats.js`(통계 로직) · `src/ui/resultScreen.js`(오버레이) · stage.js `onBossClear` · main.js `setOverlay`·`restartRun`

**완료 조건**
- 보스를 처치하면 '보스 처치 — 데모 완료' 2초 뒤 결과 화면이 뜨고 클리어 시간(m:ss.s) · 명중률(명중/발사) · 헤드샷 비율(헤드/명중) · 무기별 발사 수·비율·피해·비율·명중률 표 · 무기별 카드 3장 Lv가 숫자로 보인다
- 결과 화면에서 ESC·Tab·클릭 재잠금이 안 되고, '처음부터 다시 시작'을 누르면 사격장 시작 지점에서 '클릭하여 시작'이 뜬다
- 다시 시작 뒤 성장 탭 Lv가 전부 0이고 튜닝 값은 남아 있으며, 구역 1을 다시 클리어하면 첫 번째 강화 선택이 뜬다

---

# 6단계 — 밸런스 조정 · 퍽 개편 · 스폰 기획 · 폴리싱 (2026-09-07 사용자 에픽 4개)

5단계(T28)가 끝난 뒤 사용자가 준 에픽 4개를 티켓 9개로 나눴다. 인터뷰(2026-09-07)로 확정·제외된 항목은 각 티켓에 적었다.
**제외**: 에픽 1-4의 루트슈터형 피해 하향(현행 140 유지) · 에픽 2-1 대인 저지력(신규 카드) · 에픽 2-2 PUBG형 중총열(기존 3장 유지) · 에픽 3-1의 약 개체 B형(티어는 일반·정예 2단계).

**순서**: T41 → T42 → T44 → T43 → T45 → T46 → T47 → T48 → T49 (에픽 순. T43은 코드 없는 문서 티켓이라 에픽 1 끝에). 시작 전 인터뷰가 필요한 티켓: **T45**(카드 구성·환급 규칙), **T47**(웨이브 표 확정), **T48**(파일·라이선스), **T49**(점검 목록 승인).

## T41. CS형 반동 패턴 T자형 재설계 (에픽 1-1)
**단계** 6 · **의존** T28
`data/weapons.json` CS형 `pattern.arr`만 바꾼다. 룰(코드)은 그대로. 지금 값은 T36 ×5를 ×2.5로 되돌린 것(1~10발 누적 수직 12.8°, 11발부터 발당 0.1~0.15°, 수평 최대 ±0.85°/발).

**할 일**
- 1~10발: 수직 **현행 유지**(12.8°). 수평도 유지
- 11~30발: 수직 **발당 0.05°**(사용자 결정 2026-09-07 — 미세 유지, 30발까지 +1°) · 수평 **현행의 ×2**(±1.7°/발까지). 우 → 좌 → 우 스윙 순서는 유지 → T자(7자)
- 이론 궤적(P)으로 모양 확인. 수평 2배로 화면 밖으로 나가면 `randH`는 0.05 그대로 두고 배열만 손본다
- 무기 세팅 패널 "파일 값으로 초기화"가 새 배열을 읽는지 확인 (tuning.v1에 저장된 옛 배열이 있으면 T36 규칙대로 초기화 알림)

**완료 조건**
- CS형으로 벽에 30발을 쏘면 처음 10발은 위로 올라가고, 그 뒤 20발은 거의 같은 높이에서 좌우로 넓게 흔들린다 — 탄착군이 T자(또는 7자)로 보인다
- P 이론 궤적도 같은 모양이고, 11발 이후 좌우 폭이 전보다 두 배쯤 넓다

## T42. CS형 이동 탄퍼짐 페널티 강화 (에픽 1-2)
**단계** 6 · **의존** T41
이동 중 사격을 시스템적으로 막아 **브레이킹(카운터 스트레이핑)**을 강제한다. 지금 `moveSpreadMul` 4.0(걷기 최고속도 5.5 m/s 기준 비례).

**할 일**
- CS형 `moveSpreadMul` **4.0 → 8.0** (Claude 출발점, 수치 조정 권한 — 사격장에서 조정하고 보고). 다른 두 무기는 그대로
- 브레이킹 카드(정지 = 0.1 m/s 미만)와 감속 시간(`decelTime`)이 맞물리는지 확인: 키를 떼고 정지까지 걸리는 시간 동안 퍼짐이 얼마나 남는지 사격장에서 재고, 필요하면 CS형만 감속을 빠르게 하는 대신 **전역 값은 건드리지 않는다**(플레이어 이동은 SPEC 확정)
- 조준원이 이동 중 커지는 크기가 화면에서 분명해야 한다 (조준원은 퍼짐을 이미 반영)

**완료 조건**
- CS형으로 걸으면서 10 m 표적에 10발을 쏘면 절반 이상 빗나가고, 멈춘 직후 쏘면 대부분 맞는다
- 걷기 시작하면 조준원이 눈에 띄게 커지고 멈추면 빠르게 줄어든다
- PUBG형·루트슈터형의 이동 사격은 전과 같다

## T43. PUBG형 스탯 락인 (에픽 1-3) — 문서 티켓
**단계** 6 · **의존** T42 · **코드 변경 없음**
PUBG형의 현재 값(반동 AKM 기준 34°, 피해 190, TTK 2.3 s, 정조준 FOV 55, 교전 거리 10~25 m)을 **벤치마크 기준점으로 확정**한다.

**할 일**
- SPEC 5장 무기 표의 PUBG형 행을 "확정"으로 표기하고 값을 `data/weapons.json` 현재 값으로 맞춘다. T41·T42·T44 이후 값 비교표(docs 3종)를 재생성한다 (scratchpad compute.mjs + gen_doc.mjs)
- 이후 PUBG형 값 변경은 3-2 수치 조정 권한 밖 → 사용자 승인 필요

**완료 조건**
- SPEC 5장에 PUBG형이 "확정"으로 표시되어 있고, docs 비교표의 PUBG형 값이 게임 안 무기 세팅 패널의 파일 값과 같다

## T44. 루트슈터형 연사 탄퍼짐 (에픽 1-4, 퍼짐만)
**단계** 6 · **의존** T41 · 사용자 결정 2026-09-07: **피해는 140 유지, 반동 유지, 퍼짐만** — 연사할수록 벌어져 근접 교전 또는 점사를 유도. 목표는 **PUBG형보다 조금 더 큰 최대 퍼짐**
지금은 세 무기 모두 연사 중 퍼짐이 아예 안 자란다: 감쇠(사격 중 ×0.25)가 발 간격 동안 bloom보다 크다 (루트슈터형: 8.0 × 0.25 × 0.091 s = 0.18° > bloom 0.02°). **bloom을 감쇠보다 키워야 자란다.**

**할 일**
- 루트슈터형 `spread` — Claude 출발점(수치 조정 권한): `max` 0.8 → **2.4**(PUBG 2.0보다 큼), `bloom` 0.02 → **0.25**, `decay` 8.0 → **4.0** (발당 순증가 ≈ 0.16° → 약 13발에 최대). `base` 0.3 유지 — 초탄·점사는 정확
- 정조준 퍼짐 배율 0.5는 유지 (정조준해도 연사하면 벌어진다). 사격장 10 m·25 m에서 30발 연사 vs 3발 점사 탄착군 비교 후 값 조정, 보고
- 강화 카드 속사(rpm ×1.5)에서 발 간격이 줄어 퍼짐이 더 빨리 차는 것은 의도된 상호작용 — 성장 탭 표에는 안 나오므로 카드 설명은 그대로

**완료 조건**
- 루트슈터형으로 25 m 표적에 30발을 연사하면 10발 이후 탄착군이 눈에 띄게 퍼져 절반 가까이 빗나가고, 3발씩 끊어 쏘면 거의 다 맞는다
- 조준원이 연사 중 PUBG형 지향 사격보다 조금 더 크게 벌어지고, 멈추면 1초 안에 줄어든다
- 발당 피해·반동·정조준은 전과 같다 (무기 세팅 표의 다른 값 변화 없음)

## T45. CS형 퍽 리워크 (에픽 2-1)
**단계** 6 · **의존** T44 · **시작 시 인터뷰**
사용자 결정 2026-09-07: **대인 저지력은 쓰지 않는다.** PUBG형(2-2)·루트슈터형(2-3)은 기존 3장 그대로 — 이 티켓은 CS형만.

**확정된 것**
- **초탄 집중 = 헤드헌터 리워크**. 헤드샷 배율은 **기존 그대로**(3.5 / 5.0 / 7.0 — 에픽 문구의 50%/100%는 쓰지 않음). 탄환 환급은 "헤드샷으로 **처치**했을 때 5발"로 (기존 Lv2·3의 헤드샷마다 1·2발은 이것으로 대체 예정)
- **브레이킹 유지**

**시작 시 물을 것**
1. 카드 이름: '헤드헌터' 유지 vs '초탄 집중'으로 개명
2. 환급 5발이 붙는 레벨 (Lv3만 / Lv2부터 / Lv1부터) 및 기존 "헤드샷마다 1·2발 환급"을 완전히 없앨지
3. **멀티킬** 유지 여부 (대인 저지력이 빠져 CS형은 헤드헌터·브레이킹·멀티킬 3장이 자연스럽다 — 사용자가 "진행할 때 다시 묻기"로 보류)

**할 일 (인터뷰 뒤)**
- `src/growth/cards.js` CS형 카드 표·desc 문구 수정. 새 행동 키 `headshotKillAmmo`(헤드샷 처치 시 +N발, 탄창 상한) 추가 — shooter의 onKill에서 `hit.part === 'head'`일 때. 기존 `headshotRefund`는 인터뷰 결과에 따라 제거 또는 유지
- 성장 탭·선택 화면·결과 화면은 카드 표를 읽으므로 자동 반영. T38 검증 스크립트 갱신

**완료 조건**
- 구역 1 클리어 선택 화면의 CS형 줄에 인터뷰로 정한 카드 3장과 문구가 보인다
- 헤드헌터(또는 초탄 집중)를 찍고 몬스터를 헤드샷으로 처치하면 탄창이 5발 늘어나고(상한까지), 몸샷 처치는 늘지 않는다
- 헤드샷 배율은 전과 같다 (성장 탭 최종 열 3.5 / 5.0 / 7.0)

## T46. 몬스터 HP 티어 — 일반 · 정예 (에픽 3-1)
**단계** 6 · **의존** T28 · 사용자 확정 2026-09-07

| 티어 | 근접 HP | 원거리 HP | 비고 |
|---|---|---|---|
| 일반 | **2400** (3000→) | **1600** (2000→) | 속도·피해·행동은 그대로 |
| 정예 | **3600** | **2400** | 속도·피해 같음. 외형만 구분 |

- **구역 HP 배율 `zoneHpMul` 1.0/1.1/1.2 제거** (사용자 결정 — 정예 비율이 구역 난이도를 맡는다). 보스 소환수는 일반 티어
- 정예 외형은 Claude 결정(개발): 크기 ×1.25 + 진한 색 + HP 바 위 "정예" 표시 — 보고 후 조정

**할 일**
- `config.monster.melee/ranged`에 `hp`(일반)·`eliteHp` 추가, `zoneHpMul`·`summonHpMul` 제거(monsters.spawn hpMul 경로 정리). `monsters.spawn(kind, x, z, { elite: true })` → HP·외형 분기
- 플레이어 세팅 몬스터 그룹 슬라이더: "근접형 HP" 기본 2400, "원거리형 HP" 1600, **"정예 근접 HP" 3600, "정예 원거리 HP" 2400** 추가 (리셋 후 적용)
- 결과 화면·강화는 영향 없음. T38 검증 스크립트의 hpMul 케이스 정리

**완료 조건**
- 구역 1 몬스터를 쏘면 근접 2400 / 원거리 1600으로 죽는다 (플레이어 세팅 슬라이더 기본값이 이 숫자)
- 플레이어 세팅에서 정예 HP 슬라이더가 보이고, `__debug.monsters.spawn('melee',0,-5,{elite:true})`로 띄운 정예는 더 크고 진한 색이며 3600에 죽는다 (T47 전엔 웨이브에 안 나옴)
- 구역 3 몬스터 HP가 구역 1과 같다 (배율 제거)

## T47. 스테이지별 스폰 웨이브 표 (에픽 3-2)
**단계** 6 · **의존** T46 · **시작 시 표 확정 인터뷰**
사용자 확정 2026-09-07: **구역 1 웨이브 3에 정예 1기 첫 등장 → 구역 3 마지막 웨이브는 절반이 정예. 그 사이는 선형.** 구역 1 웨이브 1·2는 일반만.

**Claude 초안** (티켓 시작 때 사용자와 확정. 웨이브 크기는 현재 구성 유지 — 구역 1: 2/2/4, 구역 2: 3/3/6, 구역 3: 4/4/8. 정예 비율을 구역 1-3의 25 %에서 구역 3-3의 50 %까지 선형으로 올리고 마릿수는 반올림)

| 구역-웨이브 | 구성 (현행) | 정예 비율 목표 | 정예 수 | 정예 배치 |
|---|---|---|---|---|
| 1-1 | 근접 2 | 0 % | 0 | — |
| 1-2 | 원거리 2 | 0 % | 0 | — |
| 1-3 | 근접 2 + 원거리 2 | 25 % | **1** | 근접 1 |
| 2-1 | 근접 3 | 29 % | 1 | 근접 1 |
| 2-2 | 원거리 3 | 33 % | 1 | 원거리 1 |
| 2-3 | 근접 3 + 원거리 3 | 38 % | 2 | 근접 1 + 원거리 1 |
| 3-1 | 근접 4 | 42 % | 2 | 근접 2 |
| 3-2 | 원거리 4 | 46 % | 2 | 원거리 2 |
| 3-3 | 근접 4 + 원거리 4 | 50 % | **4** | 근접 2 + 원거리 2 |

**할 일**
- `src/stage/waves.js` `makeWaves`를 위 표(코드 안 테이블)로 교체 — 웨이브마다 `[{ kind, elite }]` 명시. 좌표 배치 규칙은 현행(균등)
- 정예는 웨이브 안에서 가운데 자리(플레이어가 먼저 보게)
- 상단 HUD "남은 몬스터"에 정예 수 병기 (예: `남은 몬스터 4 (정예 1)`) — Claude 결정, 보고

**완료 조건**
- 구역 1 웨이브 3에서 정예(크고 진한 색) 근접형이 1마리 나오고, 웨이브 1·2에는 없다
- 구역 2·3으로 갈수록 정예가 늘고, 구역 3 마지막 웨이브는 8마리 중 4마리가 정예다
- 표와 실제 스폰이 웨이브마다 일치한다 (HUD 정예 수로 확인)

## T48. 사운드 리소스 적용 (에픽 4-1)
**단계** 6 · **의존** T47 · **시작 시 사용자에게 파일 요청** (사용자 결정 2026-09-07)
T29 합성음을 오픈소스 음원 파일로 교체한다. 제출본이 HTML 한 파일이므로 **음원을 base64로 인라인**해야 한다 → 용량 예산이 관건.

**시작 시 물을 것 / 받을 것**
1. 음원 파일(무기 3종 격발, 타격/헤드샷, 몬스터 피격·사망(근접/원거리/보스), 웨이브·클리어·보스 등장, UI 클릭·카드 선택·확정) — 있는 것만
2. 각 파일의 출처·라이선스(CC0 / CC-BY 등). CC-BY면 표기 문구를 `docs/CREDITS.md` + 일시정지 메뉴 "크레딧"에 넣는다
3. 용량: 한 파일 ≤ 100 KB(OGG/MP3 권장), 전체 ≤ 2 MB 목표. 넘으면 어느 것을 합성음으로 남길지

**할 일**
- `src/audio/sound.js` SOUNDS 표에 `file` 항목 추가: 있으면 파일 재생(디코드 후 버퍼 캐시), 없으면 기존 합성음 폴백. 이벤트 노드는 T29 그대로(events.js)
- 음원은 `src/audio/assets/`에 두고 Vite `?url`·인라인 설정으로 `dist/index.html`에 포함. 오프라인 더블클릭 동작 확인
- 볼륨 믹싱: 파일마다 gain 값을 SOUNDS 표에 두고 마스터 볼륨과 곱함. 발사음 12 ms 스로틀 유지

**완료 조건**
- 세 무기 발사음이 서로 다르게 파일음으로 나고, 명중·처치·웨이브·클리어·카드 확정에 소리가 난다
- `npm run build`한 `dist/index.html`을 더블클릭해 오프라인에서도 같은 소리가 난다
- 소리 끄기·마스터 볼륨이 파일음에도 적용된다. 일시정지 메뉴(또는 docs)에 출처 표기가 있다

## T49. UI/UX 다듬기 (에픽 4-2)
**단계** 6 · **의존** T48 · **시작 시 점검 목록 승인** (사용자 결정 2026-09-07: Claude가 목록 제안 → 승인한 것만 작업)
시인성 · 레이아웃 정렬 · 피드백(피해 숫자, 피격 효과) 직관성.

**할 일**
- 시작 시 화면별 점검 목록을 제안한다(스크린샷 첨부): 사격장 HUD(조준원·탄약·무기 슬롯·명중률), 전투(웨이브 줄·보스 HP 바·피해 숫자 크기/색/지속·히트마커·피격 비네트·사망 오버레이), 강화 선택·결과 화면(폰트 크기·정렬·강조), 일시정지 메뉴·패널(탭 정렬·슬라이더 라벨 폭), 프롬프트(중앙 문구 겹침)
- 사용자가 고른 항목만 작업. 항목마다 전/후 스크린샷을 보고에 첨부
- 새 기능·새 화면은 넣지 않는다 (요청 밖). 색·크기·간격·문구·애니메이션 시간만

**완료 조건**
- 승인된 항목이 전부 반영되어 있고, 항목마다 사용자가 화면에서 전과 달라진 점을 확인한다
- HUD 요소가 서로 겹치지 않고(1280×720 / 1920×1080), 피해 숫자와 히트마커가 전투 중 한눈에 읽힌다

---

## 진행 현황

| 단계 | 티켓 | 상태 |
|---|---|---|
| 1 | T01~T06 | **완료** (2026-09-03, `99474ff`~`0c601b4`) |
| 2 | T07~T14 | **완료** (2026-09-03, `4940970`~`7daaba7`) |
| 3 | T15~T21 (T17.5 추가, T20 생략) | **완료** (2026-09-04, `e61bbda`~`f16b6d0`) |
| 4 | T22~T25 | **완료** (2026-09-04, `92c1743`~`4ecfac2`) |
| 5 | T26 | **완료** (2026-09-04, `ad7257c`) |
| 5(중간) | T26.1~T26.4 UI 정리 | **완료** (2026-09-05~06, `b54dc6f`~`0e78bd2`) |
| 5(중간) | T29 사운드(합성) | **완료** (2026-09-06, `095f03f`) |
| 5(중간) | T30 총기 뷰모델 · T31 반동 기본값 노출 · T32 슬라이더 범위 확대 | **완료** (2026-09-06, `ea8b6eb`~`5bda4c9`) |
| 5(중간) | T33 구역별 지형 차별화 · T34 클리어 후 다음 구역 안내 | **완료** (2026-09-07, `6016a06`·`c4f4c01`) |
| 5(중간) | T35 viewTracking·FOV 75 · T36 3정 값 교체 · T37 명중률 측정·피해 역산 | **완료** (2026-09-07, `49df9c9`~`4eb7726`) |
| 5 | T38 강화 데이터·적용 · T39 강화 선택 화면 | **완료** (2026-09-07, `750aa24`·`5e3468c`) |
| 5 | ~~T40 강화 저장·리셋~~ | **제외** (2026-09-07 사용자 결정) |
| 5 | ~~T27~~ → T38·T39 · T28 결과 화면 | **완료** (2026-09-07, `7d809f4`) — 5단계 끝 |
| 6 | T41~T44 무기 밸런스 · T45 CS 퍽 리워크 · T46~T47 몬스터 티어·웨이브 표 · T48~T49 사운드·UI/UX | 대기 (2026-09-07 추가). **다음은 T41** |
