# CLAUDE.md — 프로젝트 규칙

루트슈터 회사 지원용 **포트폴리오 데모**. 1인칭 TPS/FPS 그레이박스, 플레이 10~15분.
핵심 주제는 **"같은 AR 클래스라도 반동/퍼짐/복귀/ADS 룰만으로 완전히 다른 총이 된다"**를 3D에서 증명하는 것.

---

## 1. 작업 방식 (가장 중요)

- **티켓 하나를 끝내면 멈추고 사용자에게 확인을 요청한다.** 다음 티켓으로 자동으로 넘어가지 않는다.
  - 예외: 사용자가 "몇 번까지 이어서 해"라고 명시적으로 허가한 경우에만 연속 진행한다.
- **요청하지 않은 기능·파일·라이브러리를 추가하지 않는다.** 티켓의 "할 일"에 적힌 것만 만든다.
  - 지금 하지 않는 것: 인벤토리 확장, 퍽, 어픽스, 캐릭터 애니메이션, 레벨 아트, 스토리, 세이브, 멀티플레이.
- **애매하면 추측하지 말고 질문한다.** "합리적인 기본값"도 사용자 확인 없이 쓰지 않는다.
  - 단, 아래 3-2의 수치 조정 권한 범위 안에서는 예외.
- **새 기능은 플랜 모드에서 계획을 먼저 세우고 승인을 받은 뒤 구현한다.**
- 티켓 완료 확인은 **사용자가 로컬 서버 URL을 열어 직접 플레이**하는 방식이다.
  TASKS.md의 완료 조건은 반드시 사용자가 화면에서 직접 확인할 수 있는 문장으로 쓴다.
- **티켓 하나당 커밋 하나.** 사용자 확인을 받은 뒤 커밋한다. 커밋 메시지는 `T00 제목` 형식으로 시작한다.
- SPEC.md / TASKS.md 갱신은 **단계(1~5단계)가 끝날 때 모아서** 한다. 도중에 결정이 바뀌면 기억해 두었다가 단계 종료 시 반영하고, 무엇을 바꿨는지 보고한다.

---

## 2. 기술 구성 (확정)

| 항목 | 값 |
|---|---|
| 플랫폼 | 웹 (브라우저) |
| 렌더러 | Three.js — **npm 패키지, 버전 고정** |
| 언어 | JavaScript (ESM). TypeScript 아님 |
| 빌드 | Vite |
| 개발 실행 | `npm run dev` → 브라우저에서 URL 열어 플레이 |
| 제출본 | `npm run build` → **`dist/index.html` 파일 하나**. 더블클릭하면 실행되어야 한다 |
| 버전 관리 | git. T01에서 Claude가 `git init`과 `.gitignore`를 처리한다 |
| 폴더 구조 | **T01에서 Claude가 제안하고 사용자 승인 후 확정** |

> **제약**: 제출본은 오프라인 더블클릭으로 동작해야 한다. 런타임에 외부 네트워크를 호출하는 구조를 넣지 않는다.

---

## 3. 데이터 규칙

### 3-1. 관리 위치
- **무기 데이터만 별도 파일**로 관리한다. 몬스터·웨이브·드랍 테이블은 코드에 둔다.
- 구글 시트 연동과 시트→JSON 변환은 **하지 않는다.** 무기 데이터 파일이 곧 소스다.
- 무기 값의 **최초 출처는 `docs/총기 시뮬레이터.html`의 `WEAPONS` 배열**이다. 여기서 값을 꺼내 무기 데이터 파일의 초기값으로 삼는다.
- `docs/총기 시뮬레이터.html`은 **읽기 전용**으로 취급한다. 수정하지 않는다.
- 무기 데이터 값이 잘못되었을 때(오타·빈칸·타입 불일치)는 **화면에 경고를 띄우고 기본값으로 진행**한다. 조용히 넘어가거나 게임을 멈추지 않는다.

### 3-2. 수치 변경 권한
- **데모가 안정화될 때까지는 Claude가 수치를 자유롭게 조정해도 된다.** 단, 조정한 내용은 티켓 완료 보고에 반드시 적는다.
- 아래 SPEC.md에 "확정"으로 표기된 값은 이 권한의 대상이 아니다. 바꿔야 한다고 판단되면 **먼저 사용자에게 알리고 승인을 받는다.**
- 아직 정하지 않은 값은 임의로 채우지 말고 **SPEC.md의 TBD 목록에 쌓아둔다.** 해당 단계를 시작할 때 한 번에 인터뷰해서 결정한다.

---

## 4. 시뮬레이터 룰 이식 원칙

`docs/총기 시뮬레이터.html`이 이 프로젝트의 **반동 룰 기준 문서**다.

- 무기 데이터 **구조는 3D에 맞게 재설계해도 된다.** 단, 각 값의 **의미와 단위는 그대로 보존**한다.
  (`pattern`, `randV`, `randH`, `recovery`, `spread`, `ads`, `cap`)
- 반동과 퍼짐의 각도 값은 **FOV와 무관하게 카메라 회전 각도 그대로** 적용한다.
- 아래 네 가지 세부 룰은 **반드시 그대로 재현**한다.
  1. `cap` — 누적 수직 반동 상한 클램프
  2. 패턴 인덱스 순환 — 탄창이 패턴 길이를 넘으면 뒤 1/3 구간 반복, 사격 정지 500ms 후 인덱스 리셋
  3. 사격 중 bloom 감쇠 ×0.25
  4. 원형 균등 탄착 분포 — 각도 균등, 반지름 `sqrt(random) × 현재 퍼짐`
- `recovery`는 **"보정분 제외"** 동작이다. 총이 밀어올린 누적 반동만 되돌리고, 플레이어가 마우스로 끌어내린 조준 각도는 되돌리지 않는다.

---

## 5. 개발 순서 (변경 금지)

1. **1인칭 이동** — WASD, 마우스 룩, 감도, FOV, 벽 충돌
2. **사격 시스템** — 기준 AR 하나만 사용. 발사/연사/ADS/재장전/탄약 UI/레이캐스트/트레이서/히트마커/데미지 숫자
3. **총기별 반동 (핵심)** — 4정을 3D 사격장에 배치, 전환, 룰 재현 검증 + 문서 3종
4. **스테이지 + 몬스터** — 몬스터 2종, 트리거 존 스폰, 웨이브, 플레이어 HP/사망, 그레이박스 구역 1개
5. **스테이지 진행** — 사격장 → 구역 3개 → 보스, 클리어 조건, 드랍과 파워 상승, 결과 화면

이 순서를 바꾸지 않는다. 앞 단계가 완료 확인을 받기 전에 뒤 단계 작업을 시작하지 않는다.
