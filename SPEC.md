# SPEC.md — 루트슈터 포트폴리오 데모 기획서

작성 2026-09-03 · 인터뷰 결과 정리 · **확정**과 **TBD**를 구분해 표기

---

## 0. 데모 개요

| 항목 | 내용 | 상태 |
|---|---|---|
| 목적 | 루트슈터 회사 지원용 포트폴리오 데모 | 확정 |
| 보여줄 것 1 | 총기별 사격 감각 차이 — 같은 AR 클래스가 반동/퍼짐/복귀/ADS 룰만으로 완전히 다른 총이 된다 | 확정 |
| 보여줄 것 2 | 스테이지를 진행하며 장비로 성장하는 감각 | 확정 |
| 시점 | 1인칭 | 확정 |
| 비주얼 | 그레이박스 | 확정 |
| 플레이 시간 | 10~15분 | 확정 |
| 하지 않는 것 | 캐릭터 애니메이션, 레벨 아트, 스토리, 세이브, 멀티플레이, 인벤토리 확장, 퍽, 어픽스 | 확정 |

---

## 1. 기술 구성 — 확정

- 웹 + **Three.js (npm, 버전 고정)**, **JavaScript ESM**, **Vite**
- 개발: `npm run dev` → 로컬 서버 URL에서 직접 플레이하며 확인
- 제출: `npm run build` → **`dist/index.html` 단일 파일**, 더블클릭 실행, 오프라인 동작
- git 사용, 티켓당 커밋 1개
- **폴더 구조 — TBD**: T01에서 Claude가 제안하고 사용자 승인 후 확정

---

## 2. 무기 데이터 스키마

### 2-1. 시뮬레이터에서 계승 — 확정 (의미·단위 보존, 구조는 재설계 허용)

| 필드 | 의미 |
|---|---|
| `rpm`, `mag` | 연사 속도, 탄창 |
| `pattern` | 반동 궤적. `curve` 모드(`v0`/`vG`/`vMax`/`h0`/`hMode`/`hMax`) 또는 `array` 모드(30발 고정 배열) |
| `randV`, `randH` | 수직·수평 무작위 지터 비율. 수직은 음수로 내려가지 않음 |
| `recovery` | `delay`(ms), `speed`(°/s). **보정분 제외** — 총이 밀어올린 반동만 되돌림 |
| `spread` | `base`, `bloom`(발당 누적), `max`, `decay`(초당 회복) |
| `ads` | `allowed`, `recoil`(배율), `spread`(배율), `time`(ms). smoothstep 보간, 해제는 1.4배 빠름 |
| `cap` | `on`, `deg`. 누적 수직 반동 상한 |

**반드시 재현하는 세부 룰 — 확정**
1. `cap` 클램프
2. 패턴 인덱스 순환 (초과 시 뒤 1/3 반복, 정지 500ms 후 리셋)
3. 사격 중 bloom 감쇠 ×0.25
4. 원형 균등 탄착 분포 (`sqrt(random)` 반지름)

### 2-2. 3D를 위해 새로 추가하는 필드

| 필드 | 결정 | 값 |
|---|---|---|
| 이동 퍼짐 배율 | **확정** — 무기별로 따로 설정 가능 | TBD |
| 재장전 시간 | **확정** — 무기별 값 1개 (택티컬/빈탄창 구분 없음) | TBD |
| 헤드샷 배율 | **확정** — 무기별 값 | TBD |
| 사거리 감쇠 | **확정** — 무기별 감쇠 구간(시작 거리/끝 거리/최소 피해 비율) | TBD |
| 기본 피해량 | **확정** — 무기별 값 | TBD |

> 값 자체는 전부 TBD. 2단계 티켓 시작 시 한 번에 인터뷰해서 결정한다.

### 2-3. 데이터 관리 — 확정

- **무기만 별도 데이터 파일.** 몬스터·웨이브·드랍은 코드에 하드코딩
- 구글 시트 연동·변환 **하지 않음**. 데이터 파일이 곧 소스
- 초기값 출처: `docs/총기 시뮬레이터.html`의 `WEAPONS` 배열 (해당 파일은 읽기 전용)
- 값 오류 시 **화면 경고 + 기본값으로 진행**

---

## 3. 1단계 — 1인칭 이동

| 항목 | 값 | 상태 |
|---|---|---|
| 이동 | WASD | 확정 |
| 걷기 속도 | **5.5 m/s** | 확정 |
| 질주 | Shift, **×1.5** | 확정 |
| 웅크리기 | **C키, 토글**, **×0.5** | 확정 |
| 점프 | **없음** | 확정 |
| 가감속 | 짧은 가속·감속 (즉각 정지 아님) | 확정 |
| 눈높이 | 서있을 때 **1.7m**, 웅크렸을 때 **1.0m** | 확정 |
| 마우스 감도 | **기본 8.0, 범위 1~30**, 단위 **°/100px** (시뮬레이터와 동일) | 확정 |
| 지향 FOV | **90°** | 확정 |
| 정조준 FOV | 시뮬레이터 확대 배율 **1.77배**(PPD 26→46) 기준 | 확정 |
| 정조준 FOV 실수치 | 계산상 **약 59°** (tan 기반 환산). 인터뷰 시 제시한 "약 55°"와 차이가 있음 | **TBD — 1단계에서 확정** |
| 벽 충돌 | **벽을 따라 미끄러짐** | 확정 |

> 반동·퍼짐의 각도 값은 FOV와 무관하게 카메라 회전 각도 그대로 적용한다. — 확정

---

## 4. 2단계 — 사격 시스템 (기준 AR 하나만)

| 항목 | 값 | 상태 |
|---|---|---|
| 발사 모드 | **풀오토만** | 확정 |
| 예비 탄약 | **항상 무한** (재장전은 필요) | 확정 |
| 재장전 트리거 | R키 + **빈 탄창 상태에서 사격 입력 시 자동 재장전** | 확정 |
| 재장전 취소 조건 | 무기 교체 / 질주 / 사격 입력 / 정조준(우클릭) — **4가지 모두 가능** | 확정 |
| 정조준 입력 | 우클릭 **유지** | 확정 |
| 이동 중 사격 | **이동 속도에 비례해 퍼짐 증가** (무기별 배율) | 확정 |
| 질주 중 사격 | **불가**. 사격 버튼을 누르면 **질주가 풀리고 바로 발사**된다 | 확정 |
| 판정 | 레이캐스트. HP와 머리 콜라이더를 가진 표적 | 확정 |
| 피드백 | 트레이서, 히트마커, 데미지 숫자, 탄약 UI | 확정 |
| 표적 구성 | 몇 종을 어떤 거리에 둘지 | **TBD — 2단계에서 확정** |
| 데미지·헤드샷 배율·사거리 감쇠 구간·재장전 시간 실수치 | | **TBD — 2단계에서 확정** |

---

## 5. 3단계 — 총기별 반동 (핵심 단계)

| 항목 | 값 | 상태 |
|---|---|---|
| 탑재 총기 | **4정** — CS형 AR, PUBG형 AR, COD형 AR, 데스티니형 AR | 확정 |
| 기준 AR | **사격장에서 제외** (2단계 개발용으로만 사용, 문서에서만 언급) | 확정 |
| 무기 전환 | 숫자키 **1/2/3/4 + 마우스 휠** | 확정 |
| 검증 방법 | ① 탄착군 시각화 + 이론 패턴 오버레이 ② 화면 내 수치 패널 | 확정 |
| 검증 UI 표시 | **사격장에서는 항상 켜짐** (토글 없음) | 확정 |
| 산출물 문서 | **docs 폴더에 마크다운 3개** | 확정 |
| 문서 1 | 총기 감각 구성요소 분해 | 확정 |
| 문서 2 | 총기별 세팅 비교표 — **3D에 올린 4정만** 수록 | 확정 |
| 문서 3 | 신규 총기 기획 절차 | 확정 |
| 사격장 레이아웃 (표적 거리·배치) | | **TBD — 3단계에서 확정** |

### 4정 선정의 의미 (문서 1의 뼈대)
- **CS형** — 무작위 0%, 30발 고정 배열, ADS 없음, 기본 퍼짐 0.10°. *학습형*
- **PUBG형** — 수직은 항상 위, 수평은 무작위. 복귀 느림, ADS 350ms. *거리별 행동 분화형*
- **COD형** — 최소 반동 + 빠른 복귀, 대신 지향 퍼짐 0.50°로 조준 강제. *접근성형*
- **데스티니형** — 순한 반동 + 복귀 45ms/26°/s로 사실상 자동 리센터. *복귀 파라미터 정체성형*

---

## 6. 4단계 — 스테이지 + 몬스터 (큰 방향만)

| 항목 | 값 | 상태 |
|---|---|---|
| 몬스터 종류 | **2종 — 근접형 + 원거리형** | 확정 |
| 몬스터 요소 | 스탯, 피격, 사망 | 확정 |
| 스폰 | 트리거 존 진입 시 + 웨이브 데이터 | 확정 |
| 플레이어 | HP, 피격, 사망 | 확정 |
| 사망 처리 | **그 자리에서 즉시 부활** | 확정 |
| 구역 | 그레이박스 구역 1개 | 확정 |
| 몬스터 스탯·웨이브 구성·AI 상세 | | **TBD — 4단계에서 확정** |

---

## 7. 5단계 — 스테이지 진행 (큰 방향만)

| 항목 | 값 | 상태 |
|---|---|---|
| 구성 | 사격장 → 구역 3개 → 보스 | 확정 |
| 성장 축 | **같은 총의 수치가 오른다.** 피해량과 탄창 + **기능 해금**. 하드코딩 수준으로 단순하게 | 확정 |
| 인벤토리 | 최소한만 | 확정 |
| 결과 화면 | 클리어 시간 / 명중률·헤드샷 비율 / 총기별 사용 비율 / 획득한 장비 목록 | 확정 |
| 어떤 기능을 해금할지 | | **TBD — 5단계에서 확정** |
| 클리어 조건, 드랍 테이블, 파워 상승 폭 | | **TBD — 5단계에서 확정** |

---

## 8. TBD 목록 (한눈에)

| # | 항목 | 결정 시점 |
|---|---|---|
| 1 | 폴더 구조 | T01 |
| 2 | 정조준 FOV 실수치 (계산상 59°, 제시값 55°) | 1단계 |
| 3 | 무기 기본 피해량 | 2단계 |
| 4 | 헤드샷 배율 (무기별) | 2단계 |
| 5 | 사거리 감쇠 구간 (무기별) | 2단계 |
| 6 | 재장전 시간 (무기별) | 2단계 |
| 7 | 이동 퍼짐 배율 (무기별) | 2단계 |
| 8 | 2단계 표적 구성 | 2단계 |
| 9 | 사격장 레이아웃 | 3단계 |
| 10 | 몬스터 스탯·웨이브·AI 상세 | 4단계 |
| 11 | 해금 기능의 내용 | 5단계 |
| 12 | 클리어 조건, 드랍 테이블, 파워 상승 폭 | 5단계 |

---

## 9. 참고 문서

- `docs/총기 시뮬레이터.html` — 2D 반동 룰 시뮬레이터. 무기 값의 원본 소스이자 룰 기준. **읽기 전용**
  - 실제 파일명은 `총기 시뮬레이터.html`이다 (킥오프 문서의 `총기_시큘레이터.html`은 오기)
