# SPEC.md — 루트슈터 포트폴리오 데모 기획서

작성 2026-09-03 · 인터뷰 결과 정리 · **확정**과 **TBD**를 구분해 표기

---

## 0. 데모 개요

| 항목 | 내용 | 상태 |
|---|---|---|
| 목적 | 루트슈터 회사 지원용 포트폴리오 데모 | 확정 |
| 보여줄 것 1 | 총기별 사격 감각 차이 — 같은 AR 클래스가 반동/퍼짐/복귀/ADS 룰만으로 완전히 다른 총이 된다 | 확정 |
| 보여줄 것 2 | 스테이지를 진행하며 장비로 성장하는 감각 | 확정 |
| 시점 | 1인칭 | 확정 |
| 비주얼 | 그레이박스 | 확정 |
| 플레이 시간 | 10~15분 | 확정 |
| 하지 않는 것 | 캐릭터 애니메이션, 레벨 아트, 스토리, 세이브, 멀티플레이, 인벤토리 확장, 어픽스. ~~퍽~~ → 5단계에서 **강화 카드**(무기당 3장, 7-1)로 대체 | 확정 |

---

## 1. 기술 구성 — 확정

- 웹 + **Three.js (npm, 버전 고정)**, **JavaScript ESM**, **Vite**
- 개발: `npm run dev` → 로컬 서버 URL에서 직접 플레이하며 확인
- 제출: `npm run build` → **`dist/index.html` 단일 파일**, 더블클릭 실행, 오프라인 동작
- git 사용, 티켓당 커밋 1개
- **폴더 구조 — 확정 (T01)**: `index.html` / `src/main.js` / `src/config.js`(조절 수치 모음) / `src/core`(렌더러·루프·입력·이벤트 버스) / `src/player`(카메라·이동·충돌·시야·HP) / `src/stage`(사격장·방·웨이브·안내) / `src/ui`(패널·HUD·오버레이) / `data/`(무기) / `docs/` / `dist/`(빌드, git 제외). 이후 추가: `src/weapon`(반동·퍼짐·정조준·발사·로드아웃·뷰모델·명중률) / `src/monster` / `src/audio`(합성음) / `src/growth`(강화 카드) / `src/stats`(결과 통계)
- 제출본은 **`dist/index.html`**이다. 루트의 `index.html`은 개발용 원본이라 더블클릭하면 검은 화면이 나온다

---

## 2. 무기 데이터 스키마

### 2-1. 시뮬레이터에서 계승 — 확정 (의미·단위 보존, 구조는 재설계 허용)

| 필드 | 의미 |
|---|---|
| `rpm`, `mag` | 연사 속도, 탄창 |
| `pattern` | 반동 궤적. `curve` 모드(`v0`/`vG`/`vMax`/`h0`/`hMode`/`hMax`) 또는 `array` 모드(30발 고정 배열) |
| `randV`, `randH` | 수직·수평 무작위 지터 비율. 수직은 음수로 내려가지 않음 |
| `recovery` | `delay`(ms), `speed`(°/s). **보정분 제외** — 총이 밀어올린 반동만 되돌림 |
| `spread` | `base`, `bloom`(발당 누적), `max`, `decay`(초당 회복) |
| `ads` | `allowed`, `recoil`(배율), `spread`(배율), `time`(ms). smoothstep 보간, 해제는 1.4배 빠름 |
| `cap` | `on`, `deg`. 누적 수직 반동 상한 |

**반드시 재현하는 세부 룰 — 확정**
1. `cap` 클램프
2. 패턴 인덱스 순환 (초과 시 뒤 1/3 반복, 정지 500ms 후 리셋)
3. 사격 중 bloom 감쇠 ×0.25
4. 원형 균등 탄착 분포 (`sqrt(random)` 반지름)

### 2-2. 3D를 위해 새로 추가하는 필드

| 필드 | 결정 | 값 |
|---|---|---|
| 이동 퍼짐 배율 `moveSpreadMul` | **확정** — 무기별. 걷기 최고속도(5.5)에서 이 배율, 속도에 비례 | 기준 AR **2.0** |
| 재장전 시간 `reloadTime` | **확정** — 무기별 값 1개 (택티컬/빈탄창 구분 없음) | 기준 AR **2.0s** |
| 헤드샷 배율 `headshotMul` | **확정** — 무기별 값 | 기준 AR **1.5** |
| 사거리 감쇠 `falloff` | **확정** — 무기별 `{start, end, minRatio}` 직선 감쇠. 언제든 데이터에서 변경 | 기준 AR **20~50m, 최소 50%** |
| 기본 피해량 `damage` | **확정** — 무기별 값. 몬스터 HP는 4단계에서 이 값에 맞춰 정함 | 기준 AR **200** → 3정은 T37 역산 **190 / 190 / 140** |
| 반동 시각 추적 `viewTracking` | **확정 (T35, 2026-09-07)** — 누적 반동 중 카메라가 따라가는 비율(0~1). 탄은 항상 100%. 이론 궤적·탄착군은 탄 기준 | 3정 모두 **1.0** (CS형 0.45 설계는 T36에서 롤백) |
| 웅크리기 퍼짐 배율 `crouchSpreadMul` | **확정 (T35)** — 웅크린 동안 퍼짐에 곱함 | CS형 **0.7**, 나머지 1.0 |
| 정조준 FOV `ads.fov` | **확정 (T35)** — 무기별. 없으면 전역 59 | PUBG형 **55**, 루트슈터형 **65** (CS형은 정조준 없음) |

> 값은 2026-09-03 T07 인터뷰에서 확정. 파일: `data/weapons.json` (시뮬레이터 키 이름 유지 + 위 필드 추가). 스키마 검증 상한: `randV`·`randH` ≤ 3 (T32)

### 2-3. 데이터 관리 — 확정

- **무기만 별도 데이터 파일.** 몬스터·웨이브·드랍은 코드에 하드코딩
- 구글 시트 연동·변환 **하지 않음**. 데이터 파일이 곧 소스
- 초기값 출처: `docs/총기 시뮬레이터.html`의 `WEAPONS` 배열 (해당 파일은 읽기 전용)
- 값 오류 시 **화면 경고 + 기본값으로 진행**

---

## 3. 1단계 — 1인칭 이동

| 항목 | 값 | 상태 |
|---|---|---|
| 이동 | WASD | 확정 |
| 걷기 속도 | **5.5 m/s** | 확정 |
| 질주 | Shift, **×1.5** | 확정 |
| 웅크리기 | **C키, 토글**, **×0.5** | 확정 |
| 점프 | **없음** | 확정 |
| 가감속 | 짧은 가속·감속 (즉각 정지 아님) | 확정 |
| 눈높이 | 서있을 때 **1.7m**, 웅크렸을 때 **1.0m** | 확정 |
| 마우스 감도 | **기본 8.0, 범위 1~30**, 단위 **°/100px** (시뮬레이터와 동일) | 확정 |
| 지향 FOV | ~~90°~~ → **75°** (T35, 사용자 설계 2026-09-07 — 같은 각도가 90에서는 25% 작아 보여 반동 체감이 덜 산다). 플레이어 세팅 슬라이더 상한 170 | 확정 |
| 정조준 FOV | 시뮬레이터 확대 배율 **1.77배**(PPD 26→46) 기준 | 확정 |
| 정조준 FOV 실수치 | **무기별 `ads.fov`** — PUBG형 55° / 루트슈터형 65° (전역 기본 59°). T35에서 확정, TBD #2 종결 | 확정 (2026-09-07) |
| 정조준 이동 | 정조준 중 이동 속도 **×0.6** (`config.player.adsMoveMul`, T38 신설). PUBG형 조준 보정 Lv3만 페널티 없음 | 확정 (2026-09-07) |
| 정조준 전환 | **부드럽게 보간** (즉시 줌 아님). `ads.time`에 맞춰 FOV를 보간 — 사용자 메모 1 | 확정 |
| 질주 방향 | **앞(W)으로 갈 때만**. 옆·뒤는 걷기 속도 | 확정 (T04 인터뷰) |
| 웅크린 채 Shift | **자동으로 일어서며 질주** | 확정 (T04 인터뷰) |
| 가감속 실수치 | 가속 0.08초, 감속 0.10초, 자세 전환 0.12초 | 임시 (조정 권한) |
| 벽 충돌 | **벽을 따라 미끄러짐**. 반지름 0.35m 원 대 박스, **높이 무시**(상자 위로 오르지 않음 — 점프 없음) | 확정 |

> 반동·퍼짐의 각도 값은 FOV와 무관하게 카메라 회전 각도 그대로 적용한다. — 확정

---

## 4. 2단계 — 사격 시스템 (기준 AR 하나만)

| 항목 | 값 | 상태 |
|---|---|---|
| 발사 모드 | **풀오토만** | 확정 |
| 예비 탄약 | **항상 무한** (재장전은 필요) | 확정 |
| 재장전 트리거 | R키 + **빈 탄창 상태에서 사격 입력 시 자동 재장전** | 확정 |
| 재장전 취소 조건 | 무기 교체 / 질주 / 사격 입력 / 정조준(우클릭) — **4가지 모두 가능** | 확정 |
| 정조준 입력 | 우클릭 **유지** | 확정 |
| 이동 중 사격 | **이동 속도에 비례해 퍼짐 증가** (무기별 배율) | 확정 |
| 질주 중 사격 | **불가**. 사격 버튼을 누르면 **질주가 풀리고 바로 발사**된다 | 확정 |
| 판정 | 레이캐스트. HP와 머리 콜라이더를 가진 표적 | 확정 |
| 피드백 | 트레이서, 히트마커, 데미지 숫자, 탄약 UI | 확정 |
| 표적 구성 | **사람 표적(몸통+머리 콜라이더) 3개 + 종이 과녁판 1개.** HP **3000**(몸통 15발/머리 10발). 배치 5 / 10 / 20m. HP 0 → 뒤로 쓰러졌다 **3초 뒤 기립**, 쓰러진 동안 맞지 않음. 과녁판은 피해 없이 탄자국만 | 확정 (T13 인터뷰) |
| 조준선 | **원(링)**. 반지름 = 현재 퍼짐의 실제 화면 크기(FOV 반영). 십자선·중앙 점 없음 | 확정 (T10 사용자 지시) |
| 질주 차단 | 사격으로 풀린 질주는 **Shift를 뗐다 다시 누를 때까지** 돌아오지 않음 | 확정 (T04 인터뷰) |
| 명중 피드백 | 트레이서(총구 위치 = 카메라 기준 우 0.25·하 0.2·전방 0.4m, 0.08s) / 히트마커 X자 — **조준원 바깥에 붙고 헤드샷은 빨강**(HUD 수정 `4f89532`) / 데미지 숫자 — **조준원 오른쪽 위**(0.7s 떠오름), 강화탄은 굵은 트레이서 + 강조 숫자(T38) / 벽 탄자국 | 확정 |
| 데미지·헤드샷·감쇠·재장전·이동 퍼짐 실수치 | 2-2 표 참조 | 확정 (T07 인터뷰) |

---

## 5. 3단계 — 총기별 반동 (핵심 단계)

| 항목 | 값 | 상태 |
|---|---|---|
| 탑재 총기 | ~~4정~~ → **3정** — CS형 AR, PUBG형 AR, 루트슈터형 AR(구 데스티니형, 2026-09-07 개명). **COD형 제외** (사용자 결정 2026-09-05, `a0a3fc4` — 접근성형은 루트슈터형과 겹침) | 확정 |
| 기준 AR | **사격장에서 제외** (2단계 개발용으로만 사용, 문서에서만 언급) | 확정 |
| 무기 전환 | 숫자키 **1/2/3 + 마우스 휠**. 시작 무기는 항상 CS형 (선택 무기 비저장, T32) | 확정 |
| 반동 크기 | 사용자 설계 2026-09-07 (T36): CS형 고정 배열 **×2.5**(10발 누적 12.8°, ×5·viewTracking 0.45 설계는 롤백) / PUBG형 AKM 기준 **×2**(30발 34°, 수평 무작위 hMax 0.25) / 루트슈터형 유지(30발 8.6°) | 확정 |
| 발당 피해 | **TTK 2.0 s 기준 역산** (T37, 사용자 결정): CS 190(헤드 ×2.0) / PUBG 190(×1.5) / 루트슈터 140(×1.5). 가정 명중률 0.55 / 0.70 / 0.85, 헤드율 0.40 / 0.30 / 0.30 | 확정 (2026-09-07) |
| **PUBG형 값 락인** | T43 (2026-09-07, 6단계 에픽 1-3): `data/weapons.json` PUBG형 현재 값 전체를 **벤치마크 기준점으로 확정** — 600 rpm · 30발 · curve v0 0.72 / vG 0.052 / vMax 1.24 / hMax 0.25 random(30발 34°) · randV 0.15 / randH 0.30 · 복귀 150 ms / 10 °/s · 퍼짐 1.2 / 0.05 / 2.0 / 3.0 · 이동 ×2.0 · ADS 250 ms / 퍼짐 ×0.15 / 반동 ×1.0 / FOV 55 · 피해 190 · 헤드 ×1.5 · 교전 거리 10~25 m. **이후 PUBG형 값 변경은 3-2 수치 조정 권한 밖 → 사용자 승인.** CS형·루트슈터형은 6단계에서 계속 조정 (T41·T42·T44, 값은 docs 비교표 표 A) | **확정 (2026-09-07 T43)** |
| 무기 세팅 패널 | 튜닝 패널(T17.5) → **무기 세팅 패널**(T26.4): 스펙·핸들링·성장 탭, 슬라이더 27개 + 성장 표, `tuning.v1` 저장, 상한 확대(T32: 핸들링 ×3, 스펙 ×10), curve 반동 기본값 노출(T31) | 확정 |
| 검증 방법 | ① 탄착군 시각화(무기별 색 자국, X로 지움) + 이론 패턴 오버레이 ② ~~화면 내 수치 패널~~ **T20 생략** (사용자 결정 2026-09-04) | 확정 |
| 검증 UI 표시 | 탄착군은 항상 켜짐. **이론 궤적은 P 키 / 패널 버튼으로 온오프 (기본 켜짐)** — "토글 없음"에서 변경 (사용자 지시 2026-09-04) | 확정 |
| 산출물 문서 | **docs 폴더에 마크다운 3개** | 확정 |
| 문서 1 | 총기 감각 구성요소 분해 | 확정 |
| 문서 2 | 총기별 세팅 비교표 — **3D에 올린 3정만** 수록 (T37에서 3정 기준으로 재생성) | 확정 |
| 문서 3 | 신규 총기 기획 절차 | 확정 |
| 사격장 레이아웃 | **20 × 60 m**, 사격 라인 z 0, 표적 **10 / 25 / 50 m** (거리마다 과녁판 오른쪽 + 인간형 왼쪽, 먼 것일수록 안쪽), 우측 벽(x +10)에 탄착 패널, 바닥 거리줄 + 과녁판 위 거리 표지판 | 확정 (T17) |

### 3정 선정의 의미 (문서 1의 뼈대)
- **CS형** — 무작위 거의 0(randV/H 0.05), 30발 고정 배열 T자(T41: 10발 12.8° 수직 → 11~30발 수평 스윙), ADS 없음, 기본 퍼짐 0.15° → 연사 10발에 3.0°(T42), 이동 퍼짐 ×20 · 이동 사격 상한 ×1.25(T42). *학습형*
- **PUBG형** — 수직은 항상 위, 수평은 무작위. 복귀 느림(150 ms / 10 °/s), 지향 퍼짐 1.2°로 정조준 강제. **값 확정 (T43)**. *거리별 행동 분화형*
- **루트슈터형** — 순한 반동 + 복귀 50 ms / 25 °/s로 사실상 자동 리센터, 어디서든 쏜다. 단 연사하면 13~15발에 퍼짐 2.4°(T44) — 점사 또는 근접. *복귀 파라미터 정체성형*
- ~~COD형~~ — 접근성형은 루트슈터형과 역할이 겹쳐 제외 (2026-09-05)

---

## 6. 4단계 — 스테이지 + 몬스터 (큰 방향만)

| 항목 | 값 | 상태 |
|---|---|---|
| 몬스터 종류 | **2종 — 근접형 + 원거리형** | 확정 |
| 몬스터 요소 | 스탯, 피격, 사망 | 확정 |
| 스폰 | 트리거 존 진입 시 + 웨이브 데이터 | 확정 |
| 플레이어 | HP, 피격, 사망 | 확정 |
| 사망 처리 | **그 자리에서 즉시 부활** | 확정 |
| 구역 | 그레이박스 구역 1개 → 5단계에서 **구역 3개 + 보스 방**으로 확장 (7-1) | 확정 |

### 6-1. 4단계 인터뷰 확정 (TBD #10, 2026-09-04)

| 항목 | 값 | 상태 |
|---|---|---|
| 근접형 | HP **3000** · 속도 **7 m/s**(걷기 5.5보다 빠름) · 피해 15 · 사거리 1.8m · 준비 0.35s · 간격 1.2s | HP·속도는 확정(슬라이더), 나머지 임시 |
| 원거리형 | HP **2000** · 속도 4 · 피해 10 · **15m 거리 유지, 6m 이내면 후퇴** · 25m 안 + 시야 있으면 **빨간 조준선 → 0.8s 뒤 즉시 판정** · 간격 1.5s | HP·조준선 방식·딜레이는 확정(슬라이더), 나머지 임시 |
| 인지 | 30m 안 + 시야, 또는 피격 시 즉시 | 임시 |
| 피격 | **플래시만**(경직·넉백 없음) · **머리 박스 별도, 헤드샷 ×1.5** · HP 0 → 쓰러지고 1초 뒤 사라짐 | 확정 |
| 웨이브 | **3웨이브: 근접 2 → 원거리 2 → 근접 2 + 원거리 2** · 처치 후 **5초 카운트다운** 뒤 다음 웨이브 · 첫 웨이브는 2초(임시) | 확정 |
| 트리거 | 구역 입구(백스톱 문) 통과 시. HUD 상단 중앙에 웨이브/남은 수/카운트다운 | 확정 |
| 플레이어 HP | **1000, 자동 회복 없음** | 확정 |
| 피격 표시 | **화면 가장자리 붉은 비네트 + 맞은 방향에 붉은 호** · HP 30% 이하면 비네트 유지 · HP 바는 **화면 중앙 아래(바 + 숫자)** | 확정 |
| 사망·부활 | HP 0 → 시점 바닥으로 → **2초 뒤 같은 자리 부활, HP 가득** · 죽으면 몬스터 전부 제거, **현재 웨이브를 5초 카운트다운 뒤 다시 스폰** | 확정 |
| 구역 1 | 사격장 **백스톱 가운데 문(폭 3m)** 뒤 **30×30m 방** · **낮은 상자 1.1m**(웅크리면 가려짐) 5개 + **높은 벽 2.5m** 4개 · **몬스터 전용 단 2m**(원거리형만, 플레이어는 못 오름) · **플레이어용 높이차 없음** | 확정 (배치는 임시) |
| 레버 | 플레이어 세팅 "몬스터" 그룹: 근접/원거리 HP·속도·피해, 조준선 딜레이, 보스 HP, 플레이어 최대 HP · M = 스테이지 리셋(강화는 유지) · "명중률 (사격장)" 그룹(T37) | — |
| 구역 HP 배율 | 구역 1·2·3 몬스터 HP ×1.0 / 1.1 / 1.2, 보스 소환수 ×1.0 (T38). **6단계 T46에서 제거 → 일반·정예 티어** | 확정 → 변경 예정 |

---

## 7. 5단계 — 스테이지 진행 (큰 방향만)

| 항목 | 값 | 상태 |
|---|---|---|
| 구성 | 사격장 → 구역 3개 → 보스 | 확정 |
| 성장 축 | ~~피해량·탄창 + 기능 해금~~ → **구역 클리어마다 무기 3종 각각 강화 카드 한 장씩 선택** (7-1, T38·T39). 드랍·해금·인벤토리 없음 | 확정 (2026-09-07 사용자 설계) |
| 인벤토리 | 없음 — 무기 세팅 패널 **성장 탭**이 현재 Lv·수치를 보여준다 | 확정 |
| 결과 화면 | 클리어 시간 / 명중률·헤드샷 비율 / 총기별 사용 비율(발사·피해) / 무기별 강화 Lv (7-1, T28) | 확정 |
| 저장 | **강화는 저장하지 않는다** (새로고침 시 Lv 0). 튜닝·UI 상태만 localStorage | 확정 (T40 제외, 2026-09-07) |

### 7-1. 5단계 인터뷰 확정 (TBD #11·#12·#13, 2026-09-04 ~ 09-07)

| 항목 | 값 | 상태 |
|---|---|---|
| 구역 진행 | 모든 웨이브 처치 = 클리어 → 다음 방 **문이 열린다**(벽 사라짐). 되돌아갈 수 있다. 구역 2는 종류당 +1, 구역 3은 +2 마리 (T26) | 확정 |
| 구역 2·3 지형 | 구역 1 유지 / 구역 2 **갈림길**(20×40 m, 가운데 높은 벽) / 구역 3 **중앙 기둥 요새**(6×6×3 m 기둥 + 낮은 상자 고리). 근접형 엄폐물 틈 ≥ 2.5 m (T33) | 확정 (2026-09-07) |
| 보스 | **큰 원거리형** HP 20,000, 조준선 2개(15×2), HP 50%부터 근접형 2마리씩 10초 간격 소환(상한 4). 보스 HP 바 상단 (T26) | 확정 (2026-09-04) |
| 클리어 후 안내 | 바닥 유도선 + 문 빛기둥 + 단 뒤 통로 3 m. 문을 지나면 사라짐 (T34) | 확정 (2026-09-06) |
| 강화 카드 | **무기 3종 × 카드 3장, Lv 0~3** — 구역 1·2·3 클리어마다 세 무기 각각 한 장(총 9회, 무기당 3회). 표는 TASKS T38 (CS 헤드헌터·브레이킹·멀티킬 / PUBG 반동 제어·탄창 개조·조준 보정 / 루트슈터 속사·강화탄 사격·장탄수 회복). 네 층: 원본·튜닝·강화·최종, 반동 바닥값 40% | 확정 (2026-09-07 CSV) |
| 선택 화면 | 문이 열리기 전 게임 정지. 세 줄 다 골라야 확정, 최대 카드는 회색, ESC·Tab·전환 무시, 클리어와 동시 사망은 클리어 우선 (T39) | 확정 |
| 결과 화면 | 보스 처치 2초 뒤. 시간은 **구역 1 진입 → 보스 처치**(정지 시간 제외). 명중률 = 몬스터 명중 / 발사, 헤드율 = 헤드 / 명중. 사용 비율은 **발사 수·피해량 둘 다**. '처음부터 다시 시작' 하나 = 강화 Lv 0 + 사격장 시작 위치 + 스테이지·HP 리셋(튜닝 유지) (T28) | 확정 (2026-09-07) |

### 7-2. 5단계 중 추가된 UI·연출 (사용자 지시)

| 항목 | 값 | 티켓 |
|---|---|---|
| 상시 HUD | 설정·튜닝 패널 숨김, 중앙 프롬프트, 보스 HP 바 | T26.1 |
| 일시정지 메뉴 | 포인터 해제 시 메뉴, Tab 토글, 좌상단 상시 바(무기 세팅·플레이어 세팅·스테이지 리셋). 패널은 한 번에 하나 | T26.2 |
| 무기 전환 카드 | 전환 시 1.5초 카드 (첫 장착 제외) | T26.3 |
| 사운드 | Web Audio **합성음**(파일 없음), 이벤트 버스, 마스터 볼륨·끄기. 6단계 T48에서 파일로 교체 | T29 |
| 총기 뷰모델 | 별도 씬 고정 FOV 60, 박스 실루엣, 발사 반동·재장전·전환 연출. 끄기 가능 | T30 |

---

## 8. TBD 목록 (한눈에)

| # | 항목 | 결정 시점 |
|---|---|---|
| 1 | ~~폴더 구조~~ — **T01에서 확정** | 완료 |
| 2 | ~~정조준 FOV 실수치~~ — 무기별 `ads.fov` PUBG 55 / 루트슈터 65 (T35) | 완료 |
| 3 | ~~무기 기본 피해량~~ — 200 (T07) | 완료 |
| 4 | ~~헤드샷 배율~~ — ×1.5 (T07) | 완료 |
| 5 | ~~사거리 감쇠 구간~~ — 20~50m 최소 50% (T07) | 완료 |
| 6 | ~~재장전 시간~~ — 2.0s (T07) | 완료 |
| 7 | ~~이동 퍼짐 배율~~ — ×2.0, 무기별 필드 (T07) | 완료 |
| 8 | ~~2단계 표적 구성~~ — 사람 표적 3 + 과녁판, HP 3000, 5/10/20m, 3초 후 기립 (T13) | 완료 |
| 9 | ~~사격장 레이아웃~~ — 10/25/50m, 20×60m (T17) | 완료 |
| 10 | ~~몬스터 스탯·웨이브·AI 상세~~ — 6-1 표 (T22~T25) | 완료 |
| 11 | ~~해금 기능의 내용~~ — 해금 대신 **강화 카드 9장** (7-1, T38) | 완료 |
| 12 | ~~클리어 조건, 드랍 테이블, 파워 상승 폭~~ — 클리어 = 웨이브 전멸, 드랍 없음, 파워 상승 = 카드 Lv (7-1) | 완료 |
| 13 | ~~보스 몬스터와 구역 2·3 구성~~ — 7-1 표 (T26·T33) | 완료 |
| 14 | CS형 카드 구성 — 헤드헌터 리워크 이름·환급 5발 레벨·멀티킬 유지 여부 | 6단계 T45 시작 시 |
| 15 | 스폰 웨이브 표 (정예 비율 9칸) — TASKS T47 초안 | 6단계 T47 시작 시 |
| 16 | 음원 파일·라이선스·용량 예산 | 6단계 T48 시작 시 |
| 17 | UI/UX 점검 목록 중 작업할 항목 | 6단계 T49 시작 시 |

---

## 9. 참고 문서

- `docs/총기 시뮬레이터.html` — 2D 반동 룰 시뮬레이터. 무기 값의 원본 소스이자 룰 기준. **읽기 전용**
  - 실제 파일명은 `총기 시뮬레이터.html`이다 (킥오프 문서의 `총기_시큘레이터.html`은 오기)

---

## 10. 사용자 메모 (기록만, 해당 티켓에서 반영)

| # | 날짜 | 메모 | 반영 티켓 |
|---|---|---|---|
| 1 | 2026-09-03 | **정조준할 때 카메라가 부드럽게.** 즉시 줌되는 것은 별로 | T11 — `ads.time` 보간 (**반영 완료**) |
| 2 | 2026-09-03 | **히트했을 때 히트 마커가 뜨는 것** | T14 — X자 히트마커, 머리 구분 (**반영 완료**) |

---

## 11. 1단계 종료 기록 (2026-09-03)

- T01~T06 완료 (커밋 `99474ff` → `0c601b4`)
- 단계 중 바뀐 결정: 질주 앞으로만 / 웅크린 채 Shift → 기립 질주 / 충돌 높이 무시 / 정조준 FOV는 사용자가 슬라이더로 결정
- 잡은 버그: 첫 프레임 dt 음수 → 위치 NaN (T04에서 수정, `loop.js`)
- 규칙 변경: 개발 결정은 묻지 않고 진행, 기획만 질문 (CLAUDE.md 1장)

---

## 12. 2단계 종료 기록 (2026-09-03)

- T07~T14 완료 (커밋 `4940970` → `7daaba7`). 시뮬레이터 룰 전부 이식 완료: 패턴 누적·cap·인덱스 순환·복귀(보정분 제외)·퍼짐(bloom ×0.25, 원형 균등)·ADS(smoothstep, 1.4배 해제)
- 인터뷰 확정: TBD #3~#8 (2-2 표, 4장 표). 여전히 TBD: **#2 정조준 FOV**(59 유지, 사용자가 슬라이더로 결정 후 통보)
- 단계 중 결정: 조준선 원형(반지름 = 실제 퍼짐) / 질주 차단은 Shift 재입력까지 / 표적 HP 3000·5/10/20m·3초 기립 / 트레이서 총구 위치
- 사용자 메모 1·2 반영 완료
- 잡은 문제: 표적 배치가 낮은 엄폐물·상자·다른 표적에 가려짐 → 레이캐스트 테스트로 시선 확인 후 확정 (5m x=0.5, 10m x=3, 20m x=−4.5)
- 검증 방식 확립: Node 로직 테스트(공식 대조) + 헤드리스 Chrome 스크린샷. Claude Code 브라우저 창은 포인터 락·rAF 제한으로 실플레이 검증 불가 → 감각 확인은 사용자
- 다음: 3단계 T15부터. 시작 시 인터뷰 — 사격장 레이아웃(TBD #9)

---

## 13. 3단계 종료 기록 (2026-09-04)

- T15~T19, T21 완료 (커밋 `e61bbda` → `f16b6d0`). **T20 수치 패널은 생략** (사용자 결정 2026-09-04 — 탄착군·궤적·무기 정보 패널로 충분)
- 추가된 티켓: **T17.5 무기 튜닝 패널** — 현재 무기 값을 슬라이더로 바꿔 즉시 반영, localStorage 보존, "파일 값으로 초기화", "JSON 복사"(weapons.json 형식)
- 인터뷰 확정: TBD #9 사격장 레이아웃 (5장 표). 여전히 TBD: **#2 정조준 FOV**(59 유지)
- 단계 중 결정: 0발에서 시작한 재장전은 좌/우클릭으로 끊기지 않음(질주만) / 피해 숫자는 맞은 자리 대신 화면 우측 상단 스택 / 탄착 자국은 무기별 색(CS 빨강·PUBG 파랑·COD 초록·데스티니 주황, `src/ui/weaponColors.js`) / 이론 궤적은 P로 온오프(기본 켜짐)
- 산출물 문서 3종: `docs/총기 감각 구성요소 분해.md` · `docs/총기별 세팅 비교표.md`(표 A = weapons.json 170칸 대조) · `docs/신규 총기 기획 절차.md`
- 발견: 현재 값에서 연사 중 퍼짐 원이 실제로 자라는 총은 PUBG형뿐 (다른 3정은 발 간격 회복이 bloom을 상쇄). 시뮬레이터 룰 그대로이며 값은 바꾸지 않음
- 다음: 4단계 T22부터. 시작 시 인터뷰 — 몬스터 스탯·AI·웨이브(TBD #10)

---

## 14. 4단계 종료 기록 (2026-09-04)

- T22~T25 완료 (커밋 `92c1743` → `4ecfac2`)
- 인터뷰 확정: TBD #10 (6-1 표). 여전히 TBD: **#2 정조준 FOV**(59 유지). 새 TBD **#13 보스·구역 2·3**
- 단계 중 결정: 높이차는 플레이어용 없이 **몬스터 전용 단**으로 (T25) / 웨이브 트리거를 25m 선(T23)에서 **구역 입구 문**으로 이동(T25) / 사망 시 현재 웨이브 재스폰(T24)
- 코드: `src/monster/monsters.js`(2종, 표적과 같은 콜라이더 인터페이스, 단 위 몬스터는 floorY·bounds) · `src/stage/waves.js`(armed/countdown/wave/clear/hold 상태 기계) · `src/player/health.js` · `src/ui/playerHud.js` · `src/stage/arena.js`
- 검증 방식 추가: Claude 브라우저 창에서 `window.__debug`로 순간이동·함수 직접 호출 후 스크린샷 (게임 루프는 창이 보일 때만 돈다)
- 다음: 5단계 T26부터. 시작 시 인터뷰 — 구역 2·3 구성, 구역 간 이동, 보스(TBD #13), 클리어 조건(TBD #12)

---

## 15. 5단계 종료 기록 (2026-09-07)

- T26 → T26.1~T26.4 → T29~T32 → T35~T37 → T33·T34 → T38·T39 → T28 완료 (커밋 `ad7257c` → `7d809f4`). **T27은 T38~T40으로 대체**, **T40(강화 저장·리셋)은 제외**(사용자 결정 2026-09-07), **T20·COD형 제외** 유지
- 인터뷰 확정: TBD #2(무기별 정조준 FOV), #11·#12·#13 (7-1 표). 새 TBD **#14~#17**은 6단계 티켓 시작 시
- 단계 중 바뀐 기획 (사용자 지시): 라인업 4정 → **3정**(COD형 제외 2026-09-05) / 데스티니형 → **루트슈터형** 개명 / 지향 FOV 90 → **75** / 반동 크기 CS ×2.5·PUBG ×2 / 발당 피해 **TTK 2.0 s 역산** 190·190·140 / 성장은 드랍·해금 대신 **강화 카드** / 강화 **비저장** / 상시 HUD 비우고 일시정지 메뉴 / 선택 무기 비저장(항상 CS형)
- 발견: 세 무기 모두 **연사 중 퍼짐이 자라지 않는다**(사격 중 감쇠 ×0.25가 발 간격 동안 bloom보다 큼) — 6단계 T44에서 루트슈터형만 bloom을 키운다
- 코드: `src/growth`(카드 표·네 층 적용) · `src/ui/growthPick.js`(선택 화면) · `src/ui/resultScreen.js` + `src/stats/runStats.js`(결과) · `src/stage/arena.js`·`guide.js`(방 4개·안내) · `src/audio/sound.js`(합성음) · `src/weapon/viewModel.js`·`accuracy.js` · `src/core/events.js`(이벤트 버스) · `src/ui/pauseMenu.js`·`weaponPanel.js`·`weaponCard.js`·`prompt.js`
- 검증 방식: Node 로직 테스트(scratchpad `verify_tNN.mjs`, `file:///` 절대 URL import) + Claude 브라우저 창 `window.__debug`로 상태를 직접 세팅하고 **스크린샷으로 프레임을 돌린 뒤** 확인 (창이 숨겨지면 rAF가 멈춘다)
- 다음: **6단계 T41부터** (TASKS "6단계" 절). CLAUDE.md 5장의 개발 순서 1~5는 그대로이고 6단계는 그 뒤에 붙는다
